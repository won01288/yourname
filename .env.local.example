# Turso (libSQL) — CLAUDE.md 1장, 4장
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# Claude API — 후보 큐레이션·해설 생성 전용 (CLAUDE.md 2.2, 2.3)
ANTHROPIC_API_KEY=

# 정식 출시 전 프리미엄 작명(/naming, /api/name) 접근 제한 — 쉼표로 구분한 이메일 목록. 여기 등록된
# 이메일로 로그인한 계정만 관리자로 취급되어 위저드에 접근할 수 있다 (lib/auth.ts isAdminUser).
ADMIN_EMAILS=
