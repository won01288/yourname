# design.md — 유어네임(가칭) 디자인 시스템

> 이 문서는 디자인 관련 단일 기준 문서다. 화면·컴포넌트를 만들거나 수정할 때 반드시 먼저 읽는다.
> 색상·폰트·라운드·그림자 값을 코드에 하드코딩하지 말고, Tailwind `theme.extend` 또는 CSS 변수로 토큰화해 이 문서의 이름으로 참조한다.
> 값 조정이 필요하면 이 문서를 먼저 고치고 코드에 반영한다 (문서가 기준, 코드가 반영).

---

## 0. 디자인 철학 — "AI가 아니라 전문가처럼"

이 서비스는 두 티어로 나뉜다: **무료 "이름 점수 확인하기"**(LLM 미사용, 결정적 계산만)와 **유료·프리미엄 "프리미엄 작명"**(LLM 해설 포함). 무료 서비스는 사용자 확보를 위한 유입 채널이지만, 디자인 철학은 동일하게 적용한다 — "무료라서 가벼워 보이는" 화면을 만들지 않는다. 다만 표현 수단이 다르다: 유료 서비스는 LLM 산문(감정서 톤 해설)으로 전문성을 표현하고, 무료 서비스는 **수치·배지·막대 그래프 같은 정량 리포트**로 같은 수준의 신뢰감을 표현한다(6장 참고 — 정량 리포트도 "AI 코멘트"처럼 보이지 않게, 점수와 근거를 담담하게 제시한다).

2026년 프리미엄 SaaS 디자인의 핵심 흐름 두 가지를 이 서비스의 기본 철학으로 삼는다.

**1. 차분한 자신감(calm confidence), 복잡함이 아니라.** 2026년 최고 평가를 받는 제품들의 공통점은 기능이 많아서가 아니라 "사용자가 지금 옳은 선택을 하고 있다는 확신"을 주기 때문이다. 화면에 정보를 다 욱여넣지 않는다. 단계적으로 드러내고(progressive disclosure), 불필요한 장식·경쟁하는 시각 요소를 없앤다.

**2. AI는 보이지 않게, 결과는 전문가의 리포트처럼.** 2024년의 트렌드는 "AI 탑재"를 곳곳에 배지로 붙이는 것이었지만, 2026년 성숙한 제품은 이 언어를 감춘다. AI가 셀링포인트가 아니라 당연한 기대치가 되는 것이 성숙의 신호다. 이 서비스에서 **"AI가 생성했습니다" 같은 딱지나 챗봇 UI를 결과 화면에 노출하지 않는다.** 대신 결과 리포트는 정통 작명원 전문가가 정성껀 써준 감정서처럼 느껴져야 한다. LLM 해설문은 "AI 코멘트" 박스가 아니라 리포트 본문의 자연스러운 일부로 흐른다.

이 두 원칙이 이하 모든 규칙의 근거다.

---

## 1. 컬러 시스템

### 1.1 베이스 (라이트 모드 — 아이보리/크림)

| 토큰 | 값 | 용도 |
|---|---|---|
| `--bg-page` | `#FAF7F1` | 페이지 배경 |
| `--bg-surface` | `#FFFFFF` | 카드·패널 배경 |
| `--bg-surface-muted` | `#F3EEE4` | 보조 배경 (구분 영역) |
| `--text-primary` | `#2B2620` | 본문 텍스트 (웜 다크 그레이) |
| `--text-secondary` | `#6B6459` | 보조 텍스트 |
| `--border` | `#E6DFD1` | 기본 테두리 |

### 1.2 다크 모드 (첫 릴리스부터 지원 — 나중에 덧붙이지 않는다)

2026년 다크모드는 "나중에 대응"이 아니라 처음부터 설계에 포함하는 것이 표준이다. 모든 컬러 토큰은 라이트/다크 쌍으로 정의하고, CSS 변수로 관리해 `prefers-color-scheme` 또는 사용자 토글로 전환한다.

| 토큰 | 값 | 용도 |
|---|---|---|
| `--bg-page-dark` | `#17140F` | 페이지 배경 (웜 블랙, 순수 검정 아님) |
| `--bg-surface-dark` | `#211D17` | 카드·패널 배경 |
| `--bg-surface-muted-dark` | `#2A251D` | 보조 배경 |
| `--text-primary-dark` | `#F2ECE1` | 본문 텍스트 |
| `--text-secondary-dark` | `#B0A797` | 보조 텍스트 |
| `--border-dark` | `#3A3327` | 기본 테두리 |

- 다크모드 배경은 순수 검정(`#000`)이 아니라 웜톤이 살짝 도는 다크 브라운 계열로, 라이트모드의 아이보리 정체성을 다크에서도 유지한다.
- 다크모드에서는 유리질감(1.4 글래스모피즘)이 특히 잘 작동하므로, 모달·내비게이션에서 반투명 레이어를 적극 활용한다.

### 1.3 브랜드 악센트 (딥 퍼플/자주) — 일반 UI 전용

버튼, 링크, 강조 텍스트, 로딩, 포커스 링 등 일반 UI에만 사용한다. 오행 5색과 절대 혼용하지 않는다.

| 토큰 | 라이트 | 다크 |
|---|---|---|
| `--brand-50` | `#F3EBFA` | `#2A1F3D` |
| `--brand-100` | `#E1CCF0` | `#3D2E56` |
| `--brand-400` | `#8A5CB8` | `#9868C4` |
| `--brand-600` (기본) | `#5B3A8E` | `#8A5CB8` |
| `--brand-800` | `#3D2760` | `#C9A8E8` |

- 다크모드에서는 브랜드 컬러의 명도를 한 단계 올려서(`--brand-400`~`600` 사이) 어두운 배경 위 대비를 확보한다. 라이트모드의 `--brand-800`을 다크모드에 그대로 쓰면 배경에 묻힌다.

#### 1.3.1 앰비언트 전용 웜 골드 (2026.7.26 추가)

| 토큰 | 라이트 | 다크 |
|---|---|---|
| `--glow-amber` | `#E3B673` | `#C99A56` |

- 배경 앰비언트 글로우(3.6)에서 브랜드 퍼플과 짝을 이루는 두 번째 색으로만 사용한다. 오행 5색(1.4)과는 완전히 분리된 값이라, "오행색은 데이터 전용"이라는 규칙과 섞이지 않는다. 버튼·배지 등 일반 UI 컴포넌트에는 쓰지 않는다.

### 1.4 오행 5색 — 데이터 시각화 전용

결과 리포트의 오행 분포 차트, 용신 태그, 자원오행 배지에만 사용한다. 일반 UI(버튼·내비게이션)에는 쓰지 않는다.

| 오행 | 토큰 | 라이트 | 다크 | 계열 |
|---|---|---|---|---|
| 木 | `--element-wood` | `#6B8F5C` | `#8FB37E` | 세이지 그린 |
| 火 | `--element-fire` | `#C1614A` | `#D9826C` | 톤다운 코럴 |
| 土 | `--element-earth` | `#C08A3E` | `#D9A85E` | 머스타드 앰버 |
| 金 | `--element-metal` | `#8C8676` | `#ADA793` | 웜 그레이 |
| 水 | `--element-water` | `#2F4B6E` | `#5A7FA8` | 딥 네이비 |

- 배지·태그는 옅은 틴트 배경(라이트 10~15% opacity) + 진한 텍스트 조합으로, 화면당 오행색은 원칙적으로 1개 + 뉴트럴만 동시 노출 (오행 분포 차트 등 5색이 함께 필요한 컴포넌트는 예외).
- **예외 — 배경 시그니처 모티프 (2026.7.26 추가)**: 3.6의 오행 순환 모티프에 한해서만 오행 5색을 배경 장식에 쓸 수 있다. 이 모티프는 특정 사주의 수치를 나타내는 데이터가 아니라 "오행이 서로를 낳는다(상생)"는 개념 자체를 상징하는 장식이라 예외로 둔다. 그 외의 배경·버튼·내비게이션·카드에는 여전히 오행색을 쓰지 않는다.

### 1.5 팔레트 판단 기준 — 왜 뉴트럴 베이스인가

2026년 트렌드에서 중성적이고 톤다운된 팔레트가 강화되는 흐름과, 우리가 이미 정한 아이보리 베이스가 일치한다. 화려한 색보다 "차분하고 고급스러운" 인상이 프리미엄 AI 서비스에 더 잘 맞는다는 근거로, 이 팔레트를 임의 변경하지 않는다.

---

## 2. 타이포그래피

- **폰트**: SUIT Variable 단일 사용 (2026.7.26, Pretendard에서 교체). sun-typeface가 만든 오픈소스(SIL OFL) UI 본문 전용 서체로, Pretendard보다 획 끝이 둥글어 따뜻하고 부드러운 서비스 정서에 더 맞다고 판단해 바꿨다. Pretendard와 동일하게 npm 패키지로 자체 호스팅한다(외부 폰트 CDN에 요청을 보내지 않는다). 제목·본문 구분 없이 굵기(weight)로만 위계를 만드는 원칙은 유지.
- **굵기**: 본문 400, 강조 500, 제목 600~700. 과한 굵기 남용 금지.
- **가변 폰트(variable font) 활용**: SUIT Variable을 사용해 굵기 전환에 트랜지션을 줄 수 있게 한다 (3.3 마이크로 인터랙션 참고).

| 용도 | 크기 | 굵기 |
|---|---|---|
| 페이지 타이틀 (H1) | 28px | 700 |
| 섹션 제목 (H2) | 20px | 600 |
| 카드 제목 (H3) | 16px | 600 |
| 본문 | 15px | 400 |
| 보조/캡션 | 13px | 400 |
| 강조 이름 (결과 후보 이름 표시) | 24px | 600 |
| 랜딩 히어로 타이틀 | 40~56px (반응형) | 700 |

**키네틱 타이포그래피는 랜딩 히어로에만, 절제해서.** 2026년 실무 검증 결과 키네틱 타이포그래피(움직이는 텍스트)는 과용하면 실속 없는 폴리시가 되기 쉽다. 랜딩 페이지 히어로 타이틀의 진입 애니메이션 정도로만 제한하고, 결과 리포트나 폼 같은 실사용 화면에는 쓰지 않는다.

---

## 3. 레이아웃 & 인터랙션 패턴

### 3.1 벤토 그리드 — 결과 대시보드의 기본 레이아웃

카드 갤러리형 결과 화면을 **벤토 그리드**로 구체화한다. 벤토 그리드는 2026년에도 구조적으로 견고하다고 검증된 패턴이며(Apple, Google, MS 등 표준화), 우리 서비스의 "여러 정보를 스캔하며 파악" 요구와 잘 맞는다.

- **크기로 위계 표현**: 가장 추천하는 1순위 후보 이름은 큰 타일로, 나머지 후보는 작은 타일로 배치한다. 사용자가 스캔만 해도 우선순위가 보인다.
- **혼합 콘텐츠**: 한 벤토 그리드 안에 이름 텍스트, 오행 배지, 미니 차트(오행 분포 스파크라인 등)가 자연스럽게 섞여도 된다.
- **타일 클릭 → 드릴다운**: 타일을 클릭하면 그 타일이 상세 근거 화면으로 확장된다(아래 3.2). 별도 페이지 이동이 아니라 같은 화면 안에서의 전환이 원칙.
- **반응형**: 모바일에서는 타일이 세로로 재배열되며, 큰 타일(1순위 후보)이 항상 최상단에 온다.

### 3.2 점진적 공개 (progressive disclosure)

결과 화면은 한 번에 다 보여주지 않는다.

1. **1단계 — 벤토 요약**: 후보 이름 + 핵심 배지(용신 보완 오행, 종합 판정)만.
2. **2단계 — 타일 확장 (클릭 시)**: 아래 섹션이 순서대로, 필요할 때 펼쳐지는 아코디언 형태로 나타난다.
   - 사주 요약 (오행 분포 시각화 — 오행 5색 사용)
   - 용신 도출 근거
   - 발음오행 상생 흐름
   - 자원오행 (한자별 뜻과 오행)
   - 수리사격 (원형이정 4격 + 81수리)
   - 종합 해설문 (LLM 생성 — 단, "AI 코멘트" 박스가 아니라 감정서 본문 톤으로. 0장 철학 참조)
3. 법적 고지(CLAUDE.md 7장)는 리포트 하단에 상시 노출.

### 3.3 마이크로 인터랙션 — "정성 들여 완성되는" 느낌

프리미엄 인상은 정보량이 아니라 디테일에서 나온다. 아래를 기본 규칙으로 삼는다.

- **결과 생성 로딩**: 스피너 하나로 때우지 않는다. 계산 파이프라인의 단계(팔자 분석 → 용신 도출 → 이름 후보 탐색 → 검증)를 순차적으로 텍스트로 보여주며 진행하는 스테이지형 로딩을 쓴다. 이건 과장이 아니라 실제 결정적 파이프라인(CLAUDE.md 2장)의 단계를 그대로 보여주는 것이라 정직하기도 하다.
- **카드/타일 호버**: 살짝 떠오르는 정도(2~4px translateY + 그림자 강화)만. 과한 스케일 확대 금지.
- **타일 확장 애니메이션**: 아코디언이 펼쳐질 때 높이와 투명도를 함께 전환(200~300ms, ease-out)해 급작스럽지 않게.
- **버튼 클릭 피드백**: 배경색 전환 + 아주 미세한 scale(0.98) 정도.
- 모든 애니메이션은 `prefers-reduced-motion` 사용자 설정을 존중해 비활성화 가능해야 한다.

### 3.4 글래스모피즘 — 내비게이션과 모달에만, 절제해서

2026년 실무 피드백에 따르면 글래스모피즘을 전면적으로 쓰면 시각 노이즈가 커진다. 아래 범위로만 제한한다.

- **적용 범위**: 상단 고정 내비게이션 바, 모달/다이얼로그 배경 오버레이. 이 두 곳 외에는 사용하지 않는다.
- **스타일**: 무거운 블러가 아니라 옅은 반투명 레이어 + `backdrop-filter: blur(12~16px)` 수준. 배경색은 `--bg-surface`에 낮은 알파(85~90%)를 준 정도.
- **다크모드에서 더 효과적**: 다크 배경 위에서 반투명 레이어가 깊이감을 만들어내므로, 다크모드 내비게이션에 우선 적용한다.
- 카드나 버튼 같은 일반 컴포넌트에는 글래스 효과를 쓰지 않는다 (벤토 타일은 불투명 유지).

### 3.5 그림자 & 엘리베이션

- 순수 검정 그림자 대신, **브랜드 컬러가 살짝 섞인 옅은 컬러 섀도**를 사용한다 (예: Primary 버튼에 `0 4px 12px rgba(91, 58, 142, 0.18)`처럼 브랜드색 기반의 은은한 글로우).
- 카드 기본 그림자: `0 1px 3px rgba(0,0,0,0.06)` — 과한 드롭섀도 금지.
- 호버·확장 시에만 그림자를 한 단계 강화한다 (엘리베이션 변화로 상태 변화를 알림).

### 3.6 배경 앰비언트 & 시그니처 모티프 (2026.7.26 추가)

첫 화면(히어로)과 결과 리포트 상단이 완전한 단색이라 밋밋하다는 피드백에 따라, 아래 3개 레이어를 배경에만 겹쳐 깊이감과 브랜드 정체성을 준다. 카드·버튼 등 일반 컴포넌트는 여전히 불투명 유지(3.4와 같은 이유)하고, 이 레이어는 페이지/섹션 배경에만 적용한다 — 폼 입력 단계처럼 텍스트 판독이 중요한 곳, 후보 타일처럼 촘촘한 곳에는 쓰지 않는다.

1. **앰비언트 글로우**: 브랜드 퍼플(`--brand-400`/`--brand-600`)과 웜 골드(`--glow-amber`, 1.3.1) 계열의 큰 반경 radial-gradient 블롭 2~3개를 배경 상단에 낮은 opacity로 배치한다. 순수 장식이므로 오행색은 쓰지 않는다.
2. **그레인 텍스처**: `feTurbulence` 기반 미세 노이즈를 `mix-blend-mode: overlay`, opacity 4~6%로 겹쳐 완전 평면 배경 느낌을 없앤다.
3. **오행 순환 시그니처 모티프**: 상생(木→火→土→金→水→木) 순서로 5개 노드를 원형 배치한 얇은 라인아트. 이 5개 노드에 한해 오행 5색을 예외로 허용한다(1.4 예외 조항) — 이 모티프 자체가 "오행의 순환"이라는 개념을 상징하는 장식이지 특정 사주의 데이터가 아니기 때문이다. 아주 느리게(60~90초/1바퀴) 회전하는 정도의 절제된 모션만 두고, `prefers-reduced-motion`에서는 정지시킨다.

이 레이어는 Hero.tsx와 ResultsDashboard.tsx 상단에만 적용한다.

---

## 4. 컴포넌트 스타일

- **모서리(border-radius)**: 카드·벤토 타일 12px, 버튼·입력창 8px, 배지·태그 999px(필 형태).
- **버튼**: Primary는 `--brand-600` 배경(핵심 CTA는 `--brand-400`→`--brand-600` 그라디언트 + 브랜드 글로우도 허용) + 흰 텍스트. Secondary는 `--bg-surface` + `--border` 테두리.
- **카드 상단 액센트 바 (2026.7.26 추가)**: 화면당 가장 중요한 핵심 카드(입력 폼 카드, 만세력 카드, 사주 요약 카드)에 한해 상단에 `--brand-400`→`--brand-600` 그라디언트 3px 바를 둔다. 후보 타일 같은 보조 카드에는 쓰지 않는다 — 남용하면 3.6과 마찬가지로 장식이 정보보다 시끄러워진다.
- **로고 마크 (2026.7.26 추가)**: 워드마크("유어네임") 앞에 원 + 곡선 획 하나로 이루어진 미니 심볼(`--brand-600`)을 둔다. 내비게이션과 히어로에서 반복 노출해 브랜드를 각인시킨다.
- **입력 폼(생년월일시 등 입력)**: 최대한 단순하게. 한 화면에 필드를 몰아넣지 않고 단계별로 나누는 것을 우선 고려한다 (진입 장벽을 낮춰 완료율을 높임).
- **간격**: 8px 배수 그리드 (8/16/24/32/48px).
- **접근성**: 모든 색 조합은 WCAG AA 대비(4.5:1 이상, 본문 기준)를 만족해야 한다. 특히 오행 5색을 옅은 틴트 배경에 쓸 때 텍스트 대비를 반드시 확인한다.

---

## 6. 무료 서비스 — 랜딩 두 카드 & 점수 리포트 (2026.7.26 추가)

### 6.1 랜딩 — 두 서비스 카드

첫 화면(`/`)은 계산 로직 없이 무료 "이름 점수 확인하기"·프리미엄 "프리미엄 작명" 두 카드로 진입을 나눈다.

- 카드 레이아웃: 아이콘 + 배지(우측 상단) + 제목 + 한 줄 설명 + 필 형태 CTA 버튼. 첫 진입 화면의 핵심 CTA이므로 4장의 "핵심 카드 상단 액센트 바"(`brand-400→600` 그라디언트 3px)를 두 카드 모두에 적용해 무게감을 준다(2026.7.26 추가 — 기존엔 텍스트+화살표뿐인 약한 CTA였다가, 두 서비스가 잘 안 보인다는 피드백으로 아이콘·필 버튼·액센트 바를 더했다).
- 브랜드 컬러(`--brand-600`)는 두 카드 공통으로 쓴다. 구분은 배지·아이콘 배경 톤만으로 최소화한다 — 무료는 `--brand-50` 배경의 옅은 톤, 프리미엄은 `--brand-600` 배경의 채워진 톤(테두리도 `--brand-600`으로 살짝 강조). 카드 크기·그림자·라운드는 동일해 "무료라서 가벼워 보이는" 위계를 만들지 않는다(0장) — 프리미엄 쪽의 시각적 강조는 오직 "다음 단계로 넘어갈 만한 서비스"라는 신호일 뿐, 무료 카드를 초라하게 만들지 않는다.
- 과한 색 대비(예: 무료=회색, 프리미엄=금색 같은 별도 팔레트)는 지양한다 — 기존 팔레트 안에서만 구분한다.
- CTA는 하단에 플레인 텍스트+화살표가 아니라 배경이 채워진 필(pill) 버튼으로 둔다 — 카드 전체가 클릭 가능한 링크이지만, 시각적으로 "다음 행동"이 명확히 보이는 버튼 하나를 항상 포함한다.

### 6.2 점수/등급 표시 규칙

- 점수 숫자는 리포트에서 가장 큰 타이포로 강조한다(56px/700, H1의 28px보다 크게) — 이 서비스에서 사용자가 가장 먼저 눈에 담아야 할 값이다.
- 등급(S/A/B/C/D) 배지는 브랜드 퍼플 계열(`--brand-50` 배경 + `--brand-800` 텍스트)을 쓴다. 오행 5색은 등급 표시에 쓰지 않는다 — 1.4 "오행 5색은 데이터 시각화 전용" 원칙과 등급(성적 버킷팅)은 서로 다른 의미 축이라 섞으면 오행색의 의미가 흐려진다.
- 항목별 막대(발음오행/수리사격/자원오행/음양)는 카드 상단 액센트 바와 같은 `brand-400→brand-600` 그라디언트를 채움색으로 쓴다. 적용 불가 항목(자원오행이 NULL인 경우)은 막대를 비우고 "해당 없음"으로 표기한다 — 0점으로 보이면 "나쁜 이름"으로 오독될 수 있으므로 반드시 구분한다.

### 6.3 경고 배지 — `--status-alert` 토큰 신설

인명용 아님·불용문자 경고는 오행 5색이나 브랜드색으로 표현하지 않는다(각각 데이터 시각화·일반 UI 전용이라 의미가 겹치면 안 됨). 별도 의미 색 `--status-alert`(라이트 `#B0452E`/다크 `#D97759`, `globals.css`)를 신설해 경고 배지·경고 박스 텍스트에만 쓴다. 다른 곳(버튼·오행 배지 등)에는 쓰지 않는다.

### 6.4 로딩 컴포넌트 — stages prop 범용화

`LoadingStages`는 이제 `stages: string[]`을 prop으로 받는 범용 컴포넌트다(생략 시 유료 파이프라인 기본값). 무료(`/score`)는 LLM·후보탐색 단계가 없어 실제로 더 짧은 파이프라인을 거치므로, 각 라우트가 자신의 실제 단계만 넘긴다 — 3.3의 "과장 연출 금지, 실제 파이프라인을 그대로 보여준다" 원칙을 두 서비스 모두에 일관되게 지키기 위함이다.

---

## 7. 하지 않는 것 (2026년 기준으로 의도적으로 배제)

- **AI 챗봇 UI / "AI 생성" 배지**: 0장 철학에 따라 결과 화면에 노출하지 않는다.
- **3D 렌더링, WebGL 요소**: 성능 부담이 크고 이 서비스의 정보 신뢰성과도 맞지 않는다.
- **브루탈리즘/그런지 텍스처**: 프리미엄·신뢰 톤과 상충한다.
- **전면적 글래스모피즘**: 3.4의 제한 범위를 넘어서 카드 전체나 배경에 확대 적용하지 않는다.
- **과한 파스텔·원색 오행 컬러**: 1.4의 톤다운 값을 임의로 채도 높이지 않는다.
