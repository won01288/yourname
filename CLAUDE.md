# CLAUDE.md — AI 작명 서비스 도메인 규칙 및 개발 스펙

> 이 문서는 로컬 Claude Code가 항상 참조해야 하는 프로젝트의 단일 기준 문서(single source of truth)다.
> 작명 로직을 구현하거나 수정할 때는 반드시 이 문서의 규칙을 따른다.
> 규칙이 바뀌면 코드가 아니라 이 문서를 먼저 고친다.

---

## 0. 서비스 목표

사주 분석 기반 정통작명 전문 작명 웹 서비스. 유명 작명원에 가지 않아도 집에서 높은 품질의 작명(사주 분석 기반 정통작명)을 받을 수 있게 하는 것이 목표. 신생아 작명에 국한하지 않는다 — 사주 계산 자체가 생년월일시만 있으면 성립하는 범용 엔진이라, 개명·기존 이름 재검토 등에도 동일하게 적용된다.

- 서비스명(가칭): 유어네임
- 프로젝트/저장소 폴더명: `yourname`

핵심 사용자 흐름: **생년월일시 + 성별 + 성씨 입력 → 사주 분석 → 용신 도출 → 이름 후보 3~5개 + 근거 리포트 출력.**

### 0.1 두 서비스 티어 (Phase 7 확정, 2026.7.26)

서비스는 두 티어로 나뉜다.

- **이름 점수 확인하기 (무료)**: 생년월일시 + 이미 지어진 이름(한글+한자, "한자 찾기" 검색 UI 포함)을 입력하면 즉시 점수(0~100점)와 항목별 근거를 보여준다. **LLM을 절대 호출하지 않는다** — 결정적 계산(2.1)만으로 구성해 운영자 금전 비용이 0이 되도록 설계한다. 사용자 확보를 위한 무료 유입 채널이며, 라우트는 `/score`.
- **프리미엄 작명 (유료)**: 기존 핵심 흐름 그대로. 사주 분석 + 용신 도출로 이름 후보를 생성하고 Claude API로 해설을 만든다. 라우트는 `/naming`. (실제 결제 게이트는 아직 없다 — 6장 미결정 사항 참고.)

랜딩(`/`)은 계산 로직 없이 두 서비스로 진입을 분기하는 화면이다. 두 서비스 모두 같은 결정적 계산 엔진(`lib/naming/`)을 공유하되, 무료 서비스는 **채점**(이미 정해진 이름의 등급을 매김, `lib/naming/score.ts`)을, 유료 서비스는 **생성**(조건에 맞는 새 후보를 탐색, `lib/naming/candidates.ts`)을 한다 — 서로 다른 문제라 하드 필터(생성)와 등급 채점(점수화)의 로직이 다르다(3.10 참고).

**브랜드 리네이밍 (2026.7.26)**: 프리미엄 서비스명을 "아기 이름 짓기"에서 "프리미엄 작명"으로 바꿨다. 사주 계산에는 애초에 연령 제약이 없는데(위 0장 참고) "아기"로 한정한 프레이밍이 서비스를 실제보다 작아 보이게 만든다는 판단에 따른 것이다. 랜딩·내비게이션·입력 폼 라벨·LLM 해설 프롬프트(3.8 sajuStory, 3.9 각 후보 explanation)에서 "아기"/"신생아"/"부모" 표현을 모두 제거하고 "사용자"/"당사자"로 통일했다 — 이는 카피 변경일 뿐 2.1·2.3의 결정적/해석적 계층 경계나 계산 로직 자체에는 영향을 주지 않는다.

---

### 0.2 로그인/마이페이지 베타 (Phase 9 확정, 2026.7.28)

카카오/구글/네이버 소셜 로그인은 사업자 등록 완료 후 별도로 붙일 예정이라, 그 전 단계로 **이메일+비밀번호만 지원하는 베타 로그인**을 먼저 도입했다. 로그인의 유일한 목적은 "조회한 결과를 나중에 다시 볼 수 있게" 하는 것이다.

- **`/score`(무료)**: 로그인 없이 지금처럼 완전히 이용 가능. 로그인 상태일 때만 결과가 **자동 저장**된다(별도 "저장" 버튼 없음). 게스트는 저장·이력 없음.
- **`/naming`(프리미엄)**: 위저드 화면 자체는 비로그인도 볼 수 있지만, **최종 제출 버튼을 누르면 로그인 필요 안내**가 뜨고 API 호출 자체가 막힌다. 실제 보안 경계는 `app/api/name/route.ts`의 401(로그인 없이 호출 시 DB 조회·LLM 호출 전에 즉시 차단)이고, 클라이언트 모달(`AuthRequiredModal.tsx`)은 UX일 뿐이다.
- **보관 정책**: 이름 점수 확인 결과는 **영구 저장**(사용자가 원하면 개별 삭제). 작명 결과는 **생성 후 30일간**만 재조회 가능 — 별도 정리 배치/크론 없이 조회 시점 SQL `WHERE expires_at > CURRENT_TIMESTAMP` 필터로만 처리한다(문제가 실제로 생기기 전엔 인프라를 늘리지 않는다는 8.3 원칙).
- **인증 방식**: 비밀번호는 Node 내장 `crypto`(scrypt)로 해시하며, 새 라이브러리 의존성을 추가하지 않았다(`lib/auth.ts`). 세션은 JWT가 아니라 **DB-backed opaque 토큰**이다 — 쿠키엔 랜덤 원본 토큰을, DB(`session.id`)엔 그 SHA-256 해시만 저장해 DB가 유출돼도 세션을 재사용할 수 없게 했다. 이 방식은 새 시크릿 env var가 필요 없고 로그아웃 즉시 무효화가 가능하다.
- **비밀번호 재설정·이메일 인증은 이번 베타 범위 밖**이다(이메일 발송 인프라 자체가 없음) — 6장 미결정 사항 참고.
- 데이터 모델은 4.7, 코드 위치는 8.2를 참고.

### 0.3 SNS 로그인 (Phase 10 확정, 2026.7.31 — 같은 날 구글 제거)

사업자 등록이 완료되어, 0.2에서 "사업자 등록 완료 후 별도 작업"으로 미뤄뒀던 소셜 로그인을 붙였다. 처음엔 구글·카카오·네이버 세 곳을 붙였으나, 같은 날 **구글 연동을 제거**해 현재는 **카카오/네이버만** 지원한다.

- **라이브러리**: `next-auth`(Auth.js) v5(beta) — v5 beta에 카카오·네이버 OAuth 프로바이더가 이미 내장되어 있어(각 사별로 별도 라이브러리를 안 붙여도 됨), 별도 프로바이더 구현이 필요 없었다. Next.js 16 App Router와 호환되는 최신 메이저(v5)를 택했다 — v4는 App Router 지원이 레거시 방식(`getServerSession` + `authOptions` 중복 전달)이라 배제.
- **경계 원칙 — next-auth는 OAuth 처리 전용**: 이메일/비밀번호 로그인(0.2, DB-backed opaque 세션)은 전혀 바꾸지 않았다. next-auth는 각 제공자와의 OAuth 핸드셰이크(토큰 교환, CSRF/PKCE 방어)만 담당하고, 그 결과(내부 회원 id)를 JWT 세션에 실어 `lib/auth.ts`의 `getCurrentUser()`가 커스텀 쿠키 세션과 합쳐 하나의 `AuthUser`로 반환한다. 이 함수 하나만 확장했기 때문에 `/naming`·`/score` 게이트, 마이페이지, 관리자 판별(`isAdminUser`) 등 나머지 코드는 로그인 방식이 무엇이었는지 전혀 몰라도 된다.
- **DB 추가(둘 다 기존 데이터에 영향 없는 추가형, FK 없음 — 기존 관례와 동일)**:
  - `oauth_account`(provider, provider_account_id, user_id) — 어떤 소셜 계정이 어떤 회원과 연결되는지. **이메일이 아니라 각 제공자의 불변 고유 식별자(카카오 id·네이버 response.id)를 기준으로 매칭**한다 — 카카오는 이메일 제공에 별도 심사(사업자 등록과는 별개)가 필요해 이메일이 아예 없을 수 있기 때문.
  - `user.display_name`(nullable) — 소셜 닉네임. 이메일/비밀번호 가입자는 NULL로 남는다. 마이페이지 인사말은 `displayName ?? email`로 표시.
- **계정 자동 연동**: OAuth 프로필의 이메일이 기존 `user.email`과 일치하면 자동으로 같은 계정에 연결한다(카카오/네이버 모두 검증된 이메일만 내려주므로 안전한 가정으로 판단). 최초 로그인이면 새 `user` 행을 만든다.
- **비밀번호 없는 계정 처리**: OAuth 전용으로 생성된 `user` 행도 `password_hash NOT NULL` 제약은 그대로 지킨다 — `"oauth:v1:<provider>"` 형식의 sentinel 값을 넣는데, `verifyPassword()`가 `parts[0] !== "scrypt"`에서 즉시 false를 반환하므로 이 값으로는 이메일/비밀번호 로그인이 절대 성립하지 않는다. 컬럼 자체를 nullable로 바꾸는(SQLite 테이블 재작성이 필요한) 마이그레이션을 피하기 위한 선택.
- **카카오 이메일 미제공 시**: 이메일 없이 가입하면 `"kakao_<id>@no-email.yourname.internal"` 형식의 합성 placeholder를 `user.email`에 저장한다(UNIQUE·NOT NULL 제약 유지). 실제 이메일이 아니므로 마이페이지 등에는 `display_name`(닉네임)을 우선 표시해 이 값이 사용자 눈에 띄지 않게 한다.
- **세션 구현 — 하이브리드**: next-auth는 자체 JWT 세션(자체 쿠키)을 그대로 쓰고, 우리 쿠키(0.2)를 대체하거나 브릿지하지 않는다. `getCurrentUser()`가 "커스텀 쿠키 우선 확인 → 없으면 next-auth 세션 확인"의 순서로 통합한다. 로그아웃(`/api/auth/logout`)도 두 세션을 모두 정리한다.
- **코드 위치**: `lib/oauth.ts`(next-auth 설정, providers·jwt/session 콜백), `app/api/auth/[...nextauth]/route.ts`(handlers 노출), `next-auth.d.ts`(Session/JWT 타입 보강), `app/components/SocialLoginButtons.tsx`(로그인/회원가입 페이지의 카카오·네이버 버튼 — Server Action으로 `signIn()` 호출, 클라이언트 JS 불필요).
- **환경변수**: `AUTH_SECRET`(JWT 서명/암호화 키, 무작위 생성), `KAKAO_CLIENT_ID/SECRET`, `NAVER_CLIENT_ID/SECRET`. 콜백 URI는 `/api/auth/callback/{kakao|naver}` 형식으로 각 콘솔에 등록해야 한다. 값이 비어 있으면 버튼을 눌러도 인증 실패로 이어질 뿐 다른 기능에 영향은 없다.
- **구글 제거 (2026.7.31)**: `lib/oauth.ts`의 `Google` 프로바이더·`lib/db-auth.ts`의 `OAuthProfile.provider` 유니온·`SocialLoginButtons.tsx`의 구글 버튼·`.env.local(.example)`의 `GOOGLE_CLIENT_ID/SECRET`을 모두 제거했다. `oauth_account.provider`의 SQLite `CHECK` 제약도 `scripts/db/schema.sql`에서 `('kakao', 'naver')`로 좁혔다 — 단, 이 파일은 fresh install(새 DB에 최초 적용)에만 쓰이므로, 이미 적용된 운영 Turso DB의 기존 `CHECK` 제약(`'google'` 포함)은 소급 변경되지 않는다. SQLite는 `ALTER TABLE`로 `CHECK` 제약을 바로 못 바꾸고 테이블 재작성이 필요한데, 애초에 `GOOGLE_CLIENT_ID/SECRET`이 발급 전이라 `provider='google'` 행이 존재할 수 없으므로 굳이 이 마이그레이션을 하지 않았다 — 값 자체가 도달 불가능해졌을 뿐 실질적 위험은 없다.
- **이메일/닉네임 파싱 버그 수정 + 휴대전화번호·네이버 실명 저장 추가 (2026.8.1)**: 관리자 회원관리 화면(아래 참고)에서 네이버로 가입한 계정의 이메일이 실제 네이버 이메일이 아니라 합성 placeholder(`naver_<id>@no-email.yourname.internal`)로 저장된 것이 발견됐다. **원인**: `lib/oauth.ts`의 `jwt` 콜백이 `profile.email`/`profile.name`을 평평한(flat) 구조로 읽고 있었는데, next-auth가 콜백에 넘기는 `profile`은 provider의 `profile()` 매핑을 거치기 **전의 원본(raw) userinfo 응답**이다 — 네이버는 `{response: {id, email, name, nickname, mobile, ...}}`, 카카오는 `{kakao_account: {email, phone_number, profile: {nickname}}}` 형태로 실제 값이 한 단계 안에 중첩되어 있어, `profile.email`/`profile.name`은 두 제공자 모두 **항상 undefined**였다. 즉 이메일 동의를 정상적으로 받았어도 코드가 못 읽어 매번 placeholder가 생성됐다 — 카카오로 가입한 기존 계정들도 동일한 버그를 겪었을 가능성이 높다. **수정**: raw `profile`을 직접 파싱하는 대신, provider의 `profile()` 매핑을 이미 거쳐 표준 형태(`{id, name, email, image}`)로 넘어오는 `user` 콜백 파라미터를 이메일·닉네임의 출처로 쓰도록 바꿨다(`lib/oauth.ts`) — provider별 원본 중첩 구조를 우리 코드가 직접 알 필요가 없어져 더 안전하다.
  - **신규 수집 항목**: 위 조사 중 네이버 개발자센터에 "회원이름"·"휴대전화번호"가 필수 동의 항목으로 이미 설정되어 있었는데도 이 두 값이 애초에 수집·저장되지 않는다는 것도 함께 확인됐다(코드가 읽지도, DB 컬럼도 없었음 — 버그가 아니라 미구현). 사용자 확인 후 `user` 테이블에 `phone`(네이버 `response.mobile` / 카카오 `kakao_account.phone_number` 원문)·`real_name`(네이버 `response.name`, 회원이름/실명 — 카카오는 실명 제공 항목이 없어 항상 NULL) 컬럼을 추가했다. `display_name`(닉네임)과는 별개 값이다. 마이그레이션: `scripts/db/migrate-add-user-phone-realname.js`(ALTER TABLE, 멱등).
  - **기존 데이터 자동 정정**: 버그 수정 이전에 만들어진 계정(placeholder 이메일 등)은 코드만 고쳐서는 그대로 남는다 — `findOrCreateOAuthUser`(`lib/db-auth.ts`)가 이미 연결된 계정이면 저장된 값을 그대로 반환하고 갱신하지 않았기 때문이다. 사용자 결정에 따라 **매 로그인마다 최신 값으로 동기화**하도록 바꿨다: `displayName`/`phone`/`realName`은 `COALESCE`로 갱신(제공자가 이번 요청에 값을 안 내려줬다고 기존 확보 값을 지우지 않음), `email`은 달라졌을 때만 UPDATE하되 다른 계정이 이미 그 이메일을 쓰고 있어 UNIQUE 제약과 충돌하면(극히 드문 경우) 조용히 건너뛴다 — 그 정도로 로그인 자체를 막지 않기 위함이다. 즉 이미 가입된 사용자도 **다음 로그인 시** placeholder 이메일이 실제 이메일로 자동 교체된다.
  - **관리자 회원관리 화면 신설**: `ADMIN_EMAILS`(6장·`lib/auth.ts` `isAdminUser`)로 로그인한 관리자만 `/admin/users`에서 전체 회원의 가입 시 수집 정보(이메일·가입 방식·닉네임·회원이름·휴대전화번호·연결된 소셜 계정과 `provider_account_id`)를 그대로 나열해 볼 수 있다. 새 판정·집계를 만들지 않고 `user`/`oauth_account` 테이블 값을 그대로 옮기는 조회 전용 화면이다(`lib/db-auth.ts` `listAllUsersForAdmin`). 이 화면 자체가 이번 버그를 발견한 경로였다.

### 0.4 결제(페이앱) — 프리미엄 작명 유료화 1단계 (Phase 14 확정, 2026.8.4)

`/naming`(프리미엄 작명)에 실결제를 붙였다. 결제수단은 카드·카카오페이·네이버페이, 가격은 추천 개수(3.6.1의 `candidateCount`, 3/5/10)별로 5,900원/8,900원/14,900원이며 관리자가 `/admin/pricing`에서 바꿀 수 있다. PG사는 페이앱(PayApp)이다.

- **삼성페이는 지원하지 않는다**: 페이앱 웹 결제(JS/REST API)의 `openpaytype`에는 card/kakaopay/naverpay/applepay 등은 있지만 삼성페이는 없다 — 삼성페이는 페이앱의 오프라인 대면결제 APP SDK 전용이다. `openpaytype=card,kakaopay,naverpay` 3종만 연동한다.
- **PG사 추상화 — 토스페이먼츠 교체 대비**: `lib/payment/`(신규 폴더, `lib/naming/`과 별개 — 결제는 외부 PG/DB를 다루는 인프라라 8.1의 "LLM/Next/DB 금지" 대상이 아니다)에 `PaymentProvider` 인터페이스(`provider.ts`)를 두고, 현재는 이를 구현하는 `payapp.ts` 하나만 있다. 추후 토스페이먼츠로 바꿀 때는 `PaymentProvider`를 구현하는 `toss.ts`를 추가하고 `PAYMENT_PROVIDER` 환경변수를 `toss`로 바꾸면 된다 — `lib/db-payment.ts`·결제 API 라우트·프론트(체크아웃 위젯을 로드하는 부분 제외)는 PG사가 무엇인지 몰라도 되게 설계했다.
- **페이앱 REST가 아니라 JS SDK(lite.js)를 쓴다**: REST API(`cmd=payrequest`)는 `recvphone`(수신 휴대폰번호)이 문서상 필수인데, 이는 페이앱의 원래 용도인 "SMS로 결제링크 청구서 발송" 흐름 때문이다. 우리 서비스는 로그인 방식에 따라(이메일 가입, 카카오 로그인 등 — 0.3) 전화번호가 없는 사용자가 흔해 이 요구사항과 맞지 않는다. JS SDK는 `recvphone`이 선택사항이라 "내 사이트에서 바로 결제창 띄우기" 용도에 맞는다. 그래서 `POST /api/payment/checkout`은 페이앱 서버를 직접 호출하지 않는다 — 로컬 `payment_order`(pending)만 만들고, 프론트(`PaymentCheckoutButton.tsx`)가 `lite.payapp.kr/public/api/v2/payapp-lite.js`를 동적 로드해 직접 결제창을 연다.
- **결제 상태의 유일한 신뢰 소스는 웹훅**: 브라우저 콜백은 페이앱 문서에 명시돼 있지 않아 신뢰하지 않는다. `POST /api/payment/webhook`(페이앱 `feedbackurl`)이 `userid`/`linkkey`/`linkval`을 `.env`의 `PAYAPP_USERID`/`PAYAPP_LINKKEY`/`PAYAPP_LINKVAL`과 대조해(불일치 시 스푸핑으로 간주) `pay_state`로 상태를 갱신한다(`payment_order.status`: `pending → paid`). 응답은 반드시 순수 텍스트 `"SUCCESS"` + HTTP 200이어야 한다(문서 요구사항, 다르면 최대 10회 재시도). 프론트는 `GET /api/payment/orders/[id]`를 폴링해 웹훅이 갱신한 상태를 확인할 뿐, 그 자체로 상태를 만들지 않는다.
- **이중사용 방지 — 원자적 claim**: `app/api/name/route.ts`는 성씨 검증 등 "무료로 재시도 가능한" 입력 검증을 전부 통과한 뒤, 가장 비싼 단계(DB 풀 조회·LLM 호출) 진입 직전에 `claimPaymentOrder(orderId, userId, candidateCount)`로 `UPDATE payment_order SET status='consumed' WHERE ... AND status='paid'`를 원자적으로 실행한다. `rowsAffected`가 0이면(이미 소비됐거나, 다른 유저 소유이거나, 아직 결제가 완료되지 않음) `402 PAYMENT_REQUIRED`로 막는다 — 같은 주문으로 두 번 생성 요청(더블클릭, 새로고침 재시도)을 막는다. 이후 LLM 호출이 실패(502)하면 사용자가 결과를 못 받고도 결제를 잃는 일이 없도록 `revertPaymentOrderToPaid`로 `consumed → paid`를 되돌려 같은 주문으로 재시도할 수 있게 한다.
- **관리자 게이트는 그대로 유지**: `app/naming/page.tsx`의 `isAdminUser` 게이트(정식 출시 전 임시 제한, 5장 Phase 6~)는 이번 결제 기능과 별개로 그대로 둔다. 즉 이 결제 흐름은 현재 관리자 계정으로만 도달 가능하다 — 공개 오픈(게이트 제거) 여부는 별도 결정이다(6장).
- **관리자 가격 수정**: `/admin/pricing`(`app/admin/pricing/page.tsx`, `isAdminUser` 게이트는 `/admin/users`와 동일)에서 Server Action(`updatePriceTier` 호출)으로 `price_tier` 테이블을 직접 갱신한다 — 별도 API 라우트를 두지 않았다(8.3, `SocialLoginButtons.tsx`의 Server Action 선례를 따름). 가격 변경은 이후 새로 만들어지는 주문부터 반영되고, 이미 만든 주문의 `amount`(스냅샷)는 바뀌지 않는다.
- **취소/환불(결제 완료 후)은 이번 범위 밖**: `payment_order.status`에 `canceled`/`failed`는 두어 웹훅으로 들어오는 취소 통보를 기록하지만, 관리자가 이미 `paid`인 주문을 취소·환불"실행"하는 버튼/API는 만들지 않았다 — 필요하면 당장은 페이앱 관리자 사이트에서 직접 처리한다. (결제 완료 *전* 요청 취소는 아래 "결제 확인 중 취소" 항목에서 별도로 다룬다.)
- **결제 확인 중 취소 — 휴대폰 승인 알림까지 실제로 무효화 (2026.8.5)**: 사용자가 결제창을 연 뒤(카카오페이/네이버페이는 이 시점에 휴대폰으로 승인 알림이 감) `PaymentRequiredModal`의 "취소"를 누르면, 기존엔 모달만 닫힐 뿐 페이앱 쪽 요청은 그대로 유효해서 나중에 휴대폰에서 승인하면 결제가 그대로 성사되는데도 서비스는 이미 취소된 것으로 여겨 결과를 보여줄 방법이 없는 버그가 있었다. 페이앱 REST API `cmd=paycancel`(`mul_no` + `cancelmode=ready`로 "아직 완료 안 된 요청" 상태만 취소)로 요청 자체를 무효화하도록 고쳤다.
  - **mul_no 사전 확보**: `paycancel`을 쓰려면 결제 완료 전에 페이앱의 결제요청번호(`mul_no`)를 알아야 하는데, 페이앱 `feedbackurl`은 결제 완료 시 한 번만 오는 게 아니라 **최초 결제요청 접수 시점(`pay_state=1`)에도 먼저 한 번 호출**되며 그때 `mul_no`가 함께 온다. 웹훅(`payapp.ts parseWebhook`)이 이 상태를 새로 `"requested"`로 인식해, `payment_order.status`는 그대로 `pending`으로 두고 `provider_order_id`만 미리 채운다(`setPaymentOrderProviderOrderId`, `status='pending' AND provider_order_id IS NULL`일 때만 반영).
  - **취소 버튼 위치 이동**: "취소" UI와 확인 다이얼로그("결제 요청도 취소됩니다. 정말로 취소하시겠습니까?")를 `PaymentRequiredModal`이 아니라 `PaymentCheckoutButton` 안으로 옮겼다 — orderId·폴링 상태를 이미 그 컴포넌트가 들고 있어서다. 주문이 아직 생성 전(`orderId === null`)이면 무효화할 요청 자체가 없으므로 확인 없이 바로 닫는다.
  - **취소 API**: `POST /api/payment/orders/[id]/cancel`. `provider_order_id`가 아직 웹훅으로 안 들어왔으면 최대 2.5초 재확인 후에도 없으면 `retry: true`로 응답해 클라이언트가 다시 시도하게 한다(mul_no 없이는 취소 대상을 특정할 수 없음). 있으면 `cancelPendingRequest()` 호출 후 성공 시에만 `markOrderStatus(..., "canceled", ...)`(이 함수 자체가 `status='pending'` 가드).
  - **레이스 컨디션 처리**: 취소를 누르는 순간과 거의 동시에 휴대폰에서 승인이 끝나는 경우를 위해, 취소 API 호출 전후로 항상 DB의 최신 상태를 재조회해 최종 응답으로 삼는다. 웹훅이 먼저 `paid`로 갱신했다면 `paycancel` 호출과 무관하게(설령 실패해도) `paid`가 반환되고, 프론트(`handleCancelConfirmed`)는 이를 취소가 아니라 결제 완료로 처리해 `onPaid`로 이어간다 — 원래 버그(알림은 유효한데 화면만 닫힘)를 정확히 뒤집는 지점이다.
- **카카오톡 인앱 브라우저 대응 (2026.8.5)**: 바이럴 확산 시 카카오톡 채팅방 링크로 `/naming`에 들어오는 사용자가 늘 것으로 예상되는데, 카카오톡 인앱 브라우저(카톡 자체 미니 웹뷰)는 결제 SDK의 앱 전환·리다이렉트를 안정적으로 지원하지 않는다 — 카카오 개발자포럼에도 "인앱브라우저에서 PG결제 등이 안 되는 경우가 많다"고 공식적으로 언급돼 있고, 특히 iOS에서 카드사 앱 호출 후 복귀하지 못해 결제가 중단된 채 원래(파라미터 없는) 화면으로 되돌아가는 것이 대표 사례다. 이건 위 "결제 확인 중 취소"처럼 사용자 행동에 대응하는 문제가 아니라 웹뷰 자체의 제약이라 우리 코드로 "고칠" 수 없다 — 대신 결제가 시작되기 전, `/naming` 진입 시점에 최대한 이르게 기본 브라우저로 옮겨서 이후 흐름(로그인·작성·결제) 전체가 하나의 일관된 브라우저 컨텍스트 안에서만 일어나게 한다(컨텍스트가 중간에 바뀌며 상태가 끊기는 상황 자체를 만들지 않는 접근). `app/lib/inAppBrowser.ts`(userAgent로 카카오톡/OS 감지)·`app/components/InAppBrowserGuard.tsx`(`NamingWizardClient.tsx` 최상단에 배선)가 담당한다 — 안드로이드는 카카오 공식 스킴 `kakaotalk://web/openExternal?url=...`으로 사용자 동작 없이 즉시 기본 브라우저를 연다. iOS는 앱이 사파리를 강제로 열 수 없다는 애플 정책 제약이 있어, 대신 상단 안내 배너("우측 상단 ⋯ 메뉴에서 다른 브라우저로 열기")와 링크 복사 버튼으로 사용자가 직접 나가게 유도한다. `/score`(무료, 결제 없음)는 이 문제와 무관해 적용하지 않았다.
- **결제 완료 후 위저드 첫 화면으로 복귀하는 버그 — pending_payload 서버 스냅샷 (2026.8.5)**: 위 카카오톡 이탈 대응 이후에도(즉 Safari 등 정상 브라우저로 옮긴 뒤에도) 카카오페이/네이버페이처럼 승인 앱(카카오톡 등)을 왕복하는 결제 방식에서, 결제 완료 후 원래 탭이 아니라 다른 탭/브라우저 컨텍스트로 복귀하는 경우가 실사용에서 확인됐다 — 이 경우 모바일 풀리다이렉트 복원에 쓰던 `sessionStorage`(`PENDING_PAYMENT_KEY`)가 원래 탭에만 있어 새 컨텍스트에서는 비어 있고, `NamingWizardClient`의 `paymentReturn=1` 복원 로직이 입력값을 못 찾아 위저드 첫 화면으로 되돌아가 버렸다(결제 자체는 정상 처리됨 — "돈은 냈는데 결과를 못 받는" 유형의 버그).
  - **해결**: sessionStorage를 진짜 소스로 쓰지 않고, 결제 시작 시점의 `NameRequestPayload`를 서버(`payment_order.pending_payload`, TEXT 컬럼 신규)에 스냅샷으로 저장한다. `PaymentCheckoutButton`이 `/api/payment/checkout` 호출 시 `payload`를 함께 보내고(`PaymentRequiredModal`→`PaymentCheckoutButton`로 prop 전달), `createPaymentOrder`가 이를 저장한다. 복귀 시(`?paymentReturn=1&orderId=N`) `GET /api/payment/orders/[id]`가 저장된 값을 파싱해 `payload` 필드로 돌려주고, `NamingWizardClient`는 이제 **이 서버 응답을 우선 소스**로 위저드를 복원한다 — `orderId`만 URL에 있으면 어느 탭·어느 브라우저에서 열어도 복원되므로 컨텍스트가 바뀌는 근본 문제와 무관해진다. sessionStorage는 같은 탭이 유지되는 흔한 경우를 위한 보조 폴백(서버 조회 실패 시에만 사용)으로 남겨뒀다.
  - **DB 마이그레이션 필요**: `scripts/db/migrate-add-payment-pending-payload.js`(멱등 `ALTER TABLE`)를 실제 Turso DB에 **배포 전에** 실행해야 한다 — 새 코드의 `INSERT`가 `pending_payload` 컬럼을 참조하므로, 마이그레이션 전에 새 코드가 먼저 배포되면 결제 시작 자체가 실패한다. `schema.sql`도 함께 갱신(신규 설치용).
- **코드 위치**: `lib/payment/{types,config,provider,payapp}.ts`, `lib/db-payment.ts`(price_tier/payment_order CRUD, `setPaymentOrderProviderOrderId` 포함), `app/api/payment/{checkout,webhook,orders/[id],orders/[id]/cancel,price-tiers}/route.ts`, `app/components/{PaymentRequiredModal,PaymentCheckoutButton,InAppBrowserGuard}.tsx`, `app/lib/inAppBrowser.ts`, `app/admin/pricing/page.tsx`. `app/api/name/route.ts`·`app/lib/name-client.ts`·`app/components/InputForm.tsx`·`app/naming/NamingWizardClient.tsx`에 결제 게이트/모달을 배선했다(로그인 모달과 동일한 sessionStorage 기반 재개 패턴, 모바일 풀리다이렉트 복귀는 `returnUrl=/naming?paymentReturn=1&orderId=N`).
- **환경변수**: `PAYMENT_PROVIDER`(기본 `payapp`), `PAYAPP_USERID/LINKKEY/LINKVAL`(판매자 관리사이트 "설정 > 연동정보"), `PAYAPP_SHOPNAME`, `APP_BASE_URL`(웹훅/returnurl 절대 URL 생성용, 미설정 시 `VERCEL_URL` → `localhost:3000` 순으로 폴백 — 로컬에서 실제 웹훅을 받으려면 ngrok 등 외부 접근 가능한 URL이 필요).

### 0.5 회원 문의하기 (Phase 15 확정, 2026.8.4)

로그인한 회원이 텍스트로 문의를 남기고, 관리자가 관리자 페이지에서 답변하는 기능. 결제(0.4)처럼 별도 인프라(이메일 발송 등)를 두지 않고 기존 로그인 베타(0.2)의 DB-backed 세션 위에 테이블 하나만 얹는 최소 구현이다.

- **비공개**: 문의는 작성한 본인과 관리자만 볼 수 있다. 다른 회원에게는 노출되지 않는다 — 기존 `score_result`/`naming_result`와 동일하게 소유권을 매 조회 SQL의 `WHERE user_id = ?`로 강제한다(FK 제약 없음, 8.2 관례와 동일).
- **텍스트 전용, 영구 보관**: 문의·답변 모두 텍스트만 지원한다(첨부파일 없음). `score_result`와 같은 이유로 영구 보관하며, 자동 만료·정리 배치는 두지 않는다.
- **상태 판정**: 별도 `status` 컬럼 없이 `answer`(및 `answered_at`)가 `NULL`이면 "답변대기", 채워지면 "답변완료"로 판정한다 — 두 값이 이미 상태를 함의하므로 중복 컬럼을 두지 않는다(8.3).
- **관리자 답변**: `/admin/inquiries`(`ADMIN_EMAILS` 게이트, 기존 `/admin/users`·`/admin/pricing`과 동일)에서 답변대기 문의가 위로 정렬되어 보이고, 관리자가 텍스트로 답변을 등록하면 즉시 회원 쪽에도 노출된다. "영업일 1~3일 내 답변"은 SLA 안내 문구일 뿐 시스템이 강제하지는 않는다(예: 알림·타이머 없음) — 운영 프로세스로 지켜야 하는 약속이다.
- **삭제**: 관리자만 문의를 하드 삭제할 수 있다(`deleteInquiryAdmin`, `user_id` 조건 없음 — 어떤 회원의 문의든 삭제 가능해야 하므로). 회원 본인은 삭제할 수 없다(요청 범위 밖).
- **회원 탈퇴 연동**: `deleteUserAccount`(`lib/db-auth.ts`)에 `DELETE FROM inquiry WHERE user_id = ?`를 추가해, 탈퇴 시 다른 사용자 생성 데이터(`score_result`/`naming_result`/`session`/`oauth_account`)와 함께 정리되도록 했다.
- **코드 위치**: `lib/db-auth.ts`(`InquiryRow`/`AdminInquiryRow` + CRUD), `app/api/inquiry/route.ts`(회원의 문의 등록, POST 전용), `app/api/admin/inquiries/[id]/route.ts`(관리자의 답변 등록 PATCH·삭제 DELETE), `app/mypage/inquiries/page.tsx` + `app/components/InquiryPageClient.tsx`(회원 화면, 등록 폼 + 내 문의 목록), `app/admin/inquiries/page.tsx` + `app/components/AdminInquiryPanel.tsx`(관리자 화면). 목록 조회는 8.3 원칙대로 Server Component가 `lib/db-auth.ts`를 직접 호출하고, 클라이언트 인터랙션이 실제로 필요한 등록·답변·삭제만 API 라우트로 뒀다.

---

## 1. 기술 스택 (확정)

| 영역 | 선택 | 비고 |
|---|---|---|
| 언어 | TypeScript | |
| 프레임워크 | Next.js (App Router) | 프론트 + 계산 API 한 프로젝트 |
| 사주 계산 | `@fullstackfamily/manseryeok` | KASI 데이터, 진태양시·절기 기준 월주 지원 |
| 사주 교차검증(선택) | `lunar-javascript` (6tail) | 八字·五行·十神 대조용 |
| DB | Turso (libSQL / SQLite) | `@libsql/client` |
| LLM | Claude API (`@anthropic-ai/sdk`) | 설명 생성·후보 큐레이션 전용 |
| 배포 | Vercel | GitHub 연결 시 push마다 자동 배포. 환경변수는 프로젝트 설정에 등록 |
| 개발 환경 | VSCode + Claude Code | |

---

## 2. 핵심 설계 원칙 — 결정적 층위 vs 해석적 층위

이 서비스의 정확성은 **"무엇을 코드로 계산하고, 무엇을 LLM에 맡기는가"**의 경계를 지키는 데 달려 있다.

### 2.1 결정적 층위 (코드로만 계산 — LLM 절대 금지)

다음은 반드시 순수 함수/라이브러리/DB 조회로 처리한다. LLM에게 계산시키면 환각이 발생하므로 절대 맡기지 않는다.

- **만세력 계산**: 생년월일시 → 사주팔자(연·월·일·시주). `@fullstackfamily/manseryeok` 사용. 진태양시 보정, 절기 기준 월주, 야자시/조자시 처리 포함.
- **오행 분포 산출**: 팔자 8글자의 천간·지지·지장간을 오행으로 환산해 집계.
- **한자 획수**: DB에 저장된 원획 값 사용 (아래 3.2 참조).
- **수리사격 계산 및 81수리 길흉 판정**: 획수 조합 → 원격·형격·이격·정격 → 81수리표 대조.
- **발음오행 배속 및 상생 판정**: 초성 → 오행 매핑 → 상생 흐름 검사 (아래 3.1).
- **인명용 한자 필터**: DB의 인명용 여부 플래그로 판정.
- **불용문자 필터**: DB의 불용문자 태그로 판정.

### 2.2 해석적 층위 (규칙 엔진 + LLM)

- **신강/신약 판정, 용신 도출**: 명시적 규칙 엔진으로 처리 (학파 입장은 3.3에 고정). LLM이 아니라 규칙 코드로 구현해 일관성을 확보한다.
- **후보 순위·큐레이션, 자연어 해설문 생성**: Claude API. 단, LLM은 앞 단계에서 이미 계산·검증된 사실만 받아 서술한다. 새로운 수치나 판정을 만들어내지 않는다.

### 2.3 LLM의 역할 경계 (엄수)

LLM이 **하는 일**: 확정된 후보와 그 근거(용신, 오행 보완 내역, 획수 등)를 받아 사용자가 읽을 자연어 해설을 쓴다. 후보 간 미묘한 어감·인상을 비교해 순위를 제안한다.

LLM이 **하지 않는 일**: 사주를 세우지 않는다. 획수를 세지 않는다. 오행을 판정하지 않는다. 용신을 정하지 않는다. 인명용/불용문자 여부를 판단하지 않는다. 이 모든 것은 코드가 확정해 LLM에 전달한다.

---

## 3. 작명 규칙 (학파 결정 — 확정)

> 아래 결정들은 코드에 하드코딩하지 말고 설정 상수(config)로 분리해 나중에 바꿀 수 있게 한다.

### 3.1 발음오행 (초성 자음 → 오행) — 통설 기준

| 오행 | 자음 | 조음 |
|---|---|---|
| 木 | ㄱ, ㅋ | 牙音 |
| 火 | ㄴ, ㄷ, ㄹ, ㅌ | 舌音 |
| 土 | ㅇ, ㅎ | 喉音 |
| 金 | ㅅ, ㅈ, ㅊ | 齒音 |
| 水 | ㅁ, ㅂ, ㅍ | 脣音 |

- **결정**: ㅇ·ㅎ = 土, ㅁ·ㅂ·ㅍ = 水 (통설). 훈민정음 운해본과 반대이며, 통설을 채택함.
- 배열 검사: 성 초성 → 이름 첫 글자 초성 → 이름 끝 글자 초성의 오행이 상생 흐름(木生火·火生土·土生金·金生水·水生木)을 이루면 길. 상극은 흉.

### 3.2 획수 — 원획 기준

- **결정**: 부수를 원형 기준으로 계산하는 원획(原劃)을 사용. 예: 삼수변(氵)은 水 4획, 초두머리(艹)는 艸 6획, 손수변(扌)은 手 4획.
- 발음오행이 실제 표기 획수(필획)와 다를 수 있으므로, DB에 원획 값을 별도 컬럼으로 저장한다.

### 3.3 용신 — 억부 우선

- **결정**: 억부용신을 기본으로 도출. 신강/신약을 판정(월령·득지·득세 등 세력 계산)한 뒤, 신강이면 설기·극하는 오행을, 신약이면 생조하는 오행을 용신으로 삼는다.
- 조후용신은 한난조습이 극단적인 사주(예: 한겨울/한여름 출생으로 조후가 시급한 경우)에 한해 우선 적용을 검토한다.
- 규칙 엔진은 판정 근거(왜 이 용신인지)를 구조화된 데이터로 함께 반환해, LLM 해설과 디버깅에 쓴다.

#### 3.3.1 오행 분포 산출 — 지장간 가중치 (Phase 2 확정)

- **결정**: 지지(地支)의 지장간(支藏干)은 전통 월률분야표의 **일수 비례**로 오행에 반영한다 (예: 寅월 = 戊7일·丙7일·甲16일 → 각각 7/30, 7/30, 16/30 가중치). 천간 1글자는 가중치 1. 지지 1글자는 지장간 가중치 합이 1이 되도록 정규화해, 팔자 8글자 전체의 오행 분포 총합이 항상 8이 되게 한다.
- 지장간 일수표는 `lib/naming/config.ts`의 `BRANCH_HIDDEN_STEMS`에 상수로 고정. 유파에 따라 값이 조금씩 다르므로(예: 午월 丙丁 배분), 다른 유파 값으로 바꾸고 싶으면 이 표만 수정한다.
- 지지의 "대표 오행"(정기 기준, 월지 세력 판단 등에 사용)은 별도로 `BRANCH_ELEMENT`에 고정.

#### 3.3.2 신강/신약 판정 — 월지 가중 점수제 (Phase 2 확정)

- **결정**: 일간과 같은 오행(비겁) + 일간을 생하는 오행(인성)의 오행 분포 점수 합을 세력 점수로 삼는다. 월지가 비겁·인성에 해당하면("득령") 분자·분모에 `MONTH_BRANCH_STRENGTH_BONUS`(기본 1)를 추가로 얹어 월령의 비중을 다른 7글자보다 높게 반영한다.
- 세력 점수 ÷ 전체 점수(오행 분포 총합 8 + 월지 보너스)의 비율이 `STRENGTH_THRESHOLD`(기본 0.5) 이상이면 신강, 미만이면 신약.
- 두 상수 모두 `lib/naming/config.ts`에 있다. 실제 사주로 검수하며 임계값을 조정할 수 있게 상수로 분리했다.

### 3.4 추가 검증 규칙 (구현 시 포함)

- **인명용 한자**: 가족관계등록규칙 제37조 기준. 인명용 한자가 아니면 출생신고 시 한글로만 기록되므로, 후보에서 제외하거나 명확히 경고한다. (2024.6 기준 공식 총 9,389자. 현재 DB에는 **9,063자** 적재 — 4.1 "Phase 3 데이터 출처" 참고.)
- **불용문자**: 뜻·기운이 이름에 부적합한 한자. DB 태그로 필터. 출처: 사용자 제공 자료(`etc/nouse_hanja.csv`, 작명 실무 사이트 3곳(parkhongsam.com·dgsaju·knaming.org) 교차수집 89자, `etc/parse_nouse_hanja.py`로 파싱해 적재). 정부 공식 목록이 아니라 작명 실무 관행 자료이므로, 이 필터에 없다고 "불용문자가 아님"을 보증하지는 않는다. (2026.7.27 갱신 — 이전 출처였던 `noname_1/2.docx` 271자 파싱 결과는 완전히 폐기하고 이 CSV로 전면 교체했다. CSV 자체에 "이 항목은 불용문자 아님"이라고 명시된 龍/龜/鳳/豹/虎/鶴 6자는 제외했고, 실제 hanja DB 9,063자 중 매칭되는 88자에 `is_forbidden=1`을 반영했다.)
  - **훈(訓) 기반 자동 스캔으로 확장 (2026.7.29, 89자 → 381자)**: 3.10의 `hanja.hun`(hanjadic 출처, 7,046자)에 이름 후보 생성(`candidates.ts`)·채점(`score.ts`)이 아예 접근하지 않는다는 게 확인돼, "怨(원망할 원)·傭(머슴 용)"처럼 훈만 봐도 이름에 부적합한 한자가 89자 목록에 우연히 없다는 이유로 후보 생성 풀을 통과하는 문제가 있었다. `etc/find_negative_hun.py`(재현 가능, 사람이 정한 부정적 의미 키워드 목록으로 `hun` 문자열을 스캔)로 후보를 뽑아 사람이 전수 검토한 뒤 292자(확신도 상/중/하 + 신체특징 별도 분류 291자 + "방아 찧을 용"(舂)처럼 고전적 훈은 중립적이지만 현대 구어에서 성적 은어로 쓰여 수동으로 추가한 1자)를 `etc/nouse_hanja.csv`에 이어 붙였다. 기존 파이프라인(`parse_nouse_hanja.py` → `apply_nouse_hanja.py` → `seed-hanja.js`)을 그대로 재사용해 코드 변경 없이 데이터만 확장했다 — `getEligibleHanjaPool()`/`score.ts`/`getHanjaByReading`이 이미 `is_forbidden`을 소비하고 있어 자동 반영됐다. 최종 `is_forbidden=1`: **380자**(381자 중 hanja DB에 없는 1자 제외). 키워드 목록도 89자 원본과 동일한 한계(정부 공인 아님, 사람이 작성한 큐레이션)를 가지며, 스캔 과정에서 "천천할"(徐, 느릴)이 "천할"에, "언약할/기약할"(약속하다)이 "약할"에, "염탐할"(俔/詗/諜, 정탐하다)이 "탐할"에 우연히 걸리는 부분 문자열 오탐을 발견해 제외 처리했다(스크립트의 `EXCLUDE_EXACT_HUN`). 회귀 확인: `scripts/debug/sweep-candidates.ts`로 성씨×사주×성별 1,224개 조합 재스윕 — 후보 부족·발음 중복 0건.
  - **영문 뜻풀이(meaning) 기반 2차 스캔으로 확장 (2026.8.3, 380자 → 597자)**: `/naming` 실사용 중 "朽"(썩을 후)가 후보로 추천되는 사례가 발견됐다. 원인 조사 결과 hanjadic 원본(`etc/hanjadic_raw.txt`) 자체에 "朽=**섞을** 후"라는 오탈자가 있었다 — 훈 기반 스캔(`find_negative_hun.py`)은 `hun` 컬럼(hanjadic 출처, 위 항목 참고) 문자열만 보므로, 정확한 한글 훈("썩을")이 아니라 오타난 값("섞을")을 대조해 "썩을" 키워드에 안 걸리고 통과했다. `hun`과 독립적인 2차 출처(Unihan `meaning`, 영문 뜻풀이, 4.1)를 같은 방식(키워드 스캔 + confidence 등급 + 사람 전수 검토)으로 추가 대조하면 이런 hun 데이터 자체의 오류를 잡아낼 수 있다고 보고 `etc/find_negative_meaning.py`를 신설했다. 이미 `is_forbidden=true`인 한자는 제외하고 영문 키워드(상/중/하/신체특징 4단 확신도, 한글 키워드 목록과 대응)로 `meaning`을 스캔해 후보 CSV(`etc/negative_meaning_hanja_candidates.csv`)를 생성한다.
    - **오탐 주의**: Unihan `meaning`은 한 글자의 여러 뜻(때로 서로 무관한 훈독 조합)을 한 줄에 나열하므로, 부정적 낱말이 매칭돼도 그게 그 글자의 주된/현대 한국어 상용 의미가 아닌 경우가 예상보다 많았다(초기 상+중 후보 264자 중 37자, 약 14%) — 예: 火(오행의 "불", "분노"는 화나다의 은유일 뿐 본뜻 아님)·爛(찬란하다의 그 글자)·凜(늠름하다의 그 글자, "당당함"이라는 칭찬 표현)·踐(실천의 그 글자)·辯(변호사의 그 글자)·背/賣(등·매매라는 상용 의미가 주된 뜻이고 "배신/매국"은 특정 복합어의 부차 의미)·戩(시경에서 되레 "福"을 뜻하는 길자로도 쓰임). 이 오탐 사례를 사람이 전수 검토(`etc/merge_negative_meaning_candidates.py`의 `EXCLUDE_CHARS`)로 걸러낸 뒤, 확신도 상+중 264자 중 217자(朽 포함)를 `etc/nouse_hanja.csv`에 반영했다. 최종 `is_forbidden=1`: **597자**. 확신도 하/신체특징 등급(영문 키워드 기준)은 이번 범위에서 보류했다(회귀·오탐 검토 비용 대비 실익이 낮다고 판단, 필요시 향후 재검토).
    - 재현 파이프라인: `etc/find_negative_meaning.py`(스캔, CSV만 생성) → `etc/merge_negative_meaning_candidates.py`(사람 검토 결과 반영, `nouse_hanja.csv`에 병합) → 기존 `parse_nouse_hanja.py` → `apply_nouse_hanja.py` → `seed-hanja.js` 파이프라인 그대로 재사용. 회귀 확인: `scripts/debug/sweep-candidates.ts` 재스윕.
  - **훈(訓) 없는 한자는 후보 생성 풀에서 제외 (2026.8.3)**: `getEligibleHanjaPool()`(`lib/db.ts`, `/naming` 후보 생성 전용)에 `hun IS NOT NULL` 조건을 추가했다 — 3.10의 `getHanjaByReading()`("한자 찾기")에는 이미 적용된 규칙("훈 없이는 사용자가 뜻을 몰라 후보로 고르기 어렵다")을 후보 **생성**에도 일관되게 적용한 것뿐이다. 이미 지어진 이름을 채점하는 `getHanjaByChar()`(`/score`)는 건드리지 않았다 — 기존 이름은 훈이 없어도 그대로 채점해야 하므로 스코프를 의도적으로 분리했다. 적용 후 eligible 풀은 8,466자(9,063−597) → 6,504자로 줄었지만, 3.6.4가 지목한 구조적으로 좁은 성씨군(水초성 박·문·민·백, 金초성 서·석·신·심)까지 포함해 재스윕(1,224개 조합) 결과 후보 부족·발음 중복 0건 — 오히려 탐색 대상이 줄어 평균 처리 속도가 빨라졌다(1,430ms → 410ms/조합). DB 마이그레이션이 필요 없는 SQL 조건 변경이라 코드 배포만으로 즉시 반영된다.
  - **"이름뜻어색함" 큐레이션 신설 (2026.8.3, 685자 → 597자에서 88자 추가)**: 위 두 스캐너(`find_negative_hun.py`/`find_negative_meaning.py`)는 "부정적/흉한 뜻"만 잡아낸다. 그런데 `/naming` 생성 결과에 烏(까마귀)·葷(매운 채소)·塞(변방)·利(날카로울)처럼 나쁜 뜻은 아니지만 "이름 소재로 자연스러운가"가 애매한 하찮은 구체명사(해충·사소한 채소/조미료·신체부위·생활도구·막연한 지리군사 용어)가 섞여 나오는 문제가 별도로 있었다. `etc/find_awkward_hun.py`를 신설해 5개 카테고리(동물·음식식물·신체부위·생활용품·지리행정) 키워드로 `hun`을 스캔한다 — find_negative_hun.py와 달리 **부분 문자열이 아니라 hun 전체가 키워드와 정확히 일치할 때만** 매칭한다("무"(무=radish)가 "나무"·"무리"·"무늬"에 대량 오탐되는 것을 확인한 뒤 이렇게 바꿨다). 92자 후보 중 4자(枝/柯/條/戶)는 "가지"(채소 vs 나뭇가지)·"지게"(집 호의 옛뜻 "문" vs 운반틀) 동형이의어 오탐으로 사람 검토 후 제외했고, 나머지 88자(城·郭처럼 실제 지명·성씨로도 흔히 쓰이는 경계 사례 포함)는 사용자 확인 후 전부 반영했다. 재현 파이프라인: `etc/find_awkward_hun.py`(스캔) → `etc/merge_awkward_hun_candidates.py`(검토 결과 병합) → 기존 `parse_nouse_hanja.py` → `apply_nouse_hanja.py` → `seed-hanja.js`. 회귀 확인: 훈없음 필터와 동일하게 좁은 성씨군 우선 확인(`scripts/debug/check-hun-filter-impact.ts`) + 전체 재스윕, 둘 다 문제 없음.
  - **"이름뜻어색함" 카테고리 확장 (2026.8.4, 685자 → 726자로 41자 추가)**: 100개 샘플 재확인 중 利(날카로울)·銹(동록/녹슬) 같은, 위 5개 카테고리 어디에도 안 걸리는 "물성 형용사/부식" 계열이 여전히 남아있는 게 확인돼, `find_awkward_hun.py`에 5개 카테고리(감각촉감·부식노화·거친자연현상·무기흉기·생리현상)를 추가했다(총 10개). 59자 후보가 나왔는데, 검토 중 **한국어 짧은 단어의 동형이의어 문제**를 3건 발견했다 — 부분 문자열이 아니라 exact-match를 쓰는데도 걸러지지 않는 종류다: "쓸"(苦=쓴맛)이 매울맛과 무관한 "쓰다/쓸다"(用·書·庸·掃·埽·費, 무려 6자 — 書는 "책 서"의 그 글자)에, "짤"(鹹=짠맛)이 "짜다"(搾·絍·織 — 織는 "조직"의 그 글자)에, "창"(槍=무기)이 "창문"(窓·窗)에 걸렸다. 屢(자주, "누차"의 그 글자)는 아예 무관한 뜻인데 훈 데이터 자체가 "창"으로 잘못 매핑된 것으로 보인다. 이 18자(위 9자 + 3.6.4부터 반복 등장하는 枝·柯·條·戶 4자 + 策·辛)는 제외하고, 나머지 41자(霜(서리)처럼 雪과 유사하게 시적으로도 쓰일 수 있는 경계 사례 포함)는 사용자 확인 후 전부 반영했다. 최종 `is_forbidden=1`: **726자**. 재현 파이프라인은 동일하되 병합 스크립트만 `etc/merge_awkward_hun_candidates_v2.py`로 분리(이전 라운드 스크립트를 그대로 재실행하면 이전 EXCLUDE_CHARS만 적용되므로). 회귀 확인: 좁은 성씨군 우선 확인 + 전체 재스윕, 문제 없음.
- **음양 배열**: 획수의 홀(양)/짝(음) 균형 검토.
- **현대 어감**: 놀림감·부정적 연상·로마자 표기 문제 검토 (LLM 보조 가능).

### 3.5 자원오행(한자 부수 → 오행) 배속 — 영구적 부분표 (Phase 3 확정, 조사 완료)

- **조사 결론(2026.7.25)**: "214부수 전체를 배속한 업계 표준표"는 **애초에 존재하지 않는다.** 전문 작명원 사이트조차 자체 배속표를 공개하지 않고 유료 참고서적(예: 「作名玉篇 五行漢字典」)을 보라고 안내한다 — 실무는 "부수→오행 214행 표"가 아니라 **글자 하나하나를 판단한 특정 저자의 자전**으로 존재한다. 자원오행 이론 자체가 학술적으로 검증되지 않았다는 비판적 시각도 있다.
- **실증 사례**: 山(뫼 산)·石(돌 석)은 흔히 드는 예인데, 실제로 한 학술 자료는 石을 金으로 분류하는 반면 다른 자료는 土로 분류한다 — 우리 표도 처음엔 石을 土로 넣어뒀다가 이 충돌을 확인하고 **뺐다**.
- **결정**: 부수 의미가 논쟁의 여지 없이 하나의 오행에 대응하는 경우만 배속한다 (木火土金水 그 자체와, 식물/불/흙/쇠/물이라는 본뜻이 명확한 극소수 확장). 강희자전 214부수 중 **18개**만 채운 표이며(`lib/naming/config.ts`의 `RADICAL_ELEMENT`, 부수번호 키), 나머지 부수의 한자는 `hanja.element = NULL`이다.
- 王/玉(금 vs 토), 石(금 vs 토), 酉(수 vs 금)처럼 출처마다 배속이 갈리는 부수는 일부러 넣지 않았다. 잘못된 배속을 넣는 것보다 비워두는 쪽이 2.1의 "환각 금지" 원칙에 맞다.
- **이 표는 "미완성이라 나중에 채울 표"가 아니라 "영구적으로 부분적인 표"다.** 더 채우려면 특정 참고서적·유파 하나를 선택해 그것을 전부 따르는 결정이 필요하다 (6장 참고). 그 결정이 없는 한 이 상태를 유지한다.
- **2단계(글자 의미 기반) 보완 로직은 만들지 않는다 (2026.7.25 확정)**: 사주를 자원오행으로 보완할 때 "부수 오행이 없으면 글자 자체의 뜻으로 오행을 판단"하는 2순위 로직을 검토했으나, 채택하지 않는다. 이유: (1) 글자 뜻→오행 판단은 부수보다 더 주관적이고 유파마다 갈리는 영역이라 규칙화가 안 된다(위 조사 결론과 동일한 문제), (2) 이를 자동화하려면 사실상 LLM이 오행을 판정하게 되는데 이는 2.3 "LLM은 오행을 판정하지 않는다"와 정면으로 부딪힌다. **결정**: `hanja.element`가 `NULL`인 한자는 자원오행 조건 자체를 건너뛴다(그 한자가 "조건을 못 채운다"가 아니라 "이 기준으로는 판단하지 않는다"로 취급) — 후보는 발음오행·수리 등 다른 기준으로만 평가된다. Phase 4 구현 시 이 규칙을 그대로 따를 것.

### 3.6 후보 생성 — 다양성 선택 (Phase 6 확정, 2026.7.25 버그 수정)

- **발견된 문제(베타 실사용 확인, 2026.7.25)**: `buildCandidates`가 하드 필터(발음오행 상생 + 4격 전부 길수) 통과 후 점수(자원오행 일치 0~2 + 음양균형 0/1, 가능한 값이 6가지뿐)로만 정렬해 상위 5개를 그대로 잘라냈다. 같은 (원격,형격,이격,정격) 획수쌍 안에서 조건을 만족하는 한자 조합이 수십 개씩 동점을 이루는 게 흔한데, `Array.sort`가 안정정렬이라 동점자는 DB 삽입 순서 그대로 남았다. 그 결과 실사용에서 **추천 이름 5개의 첫 글자가 전부 동일**하거나(예: 5개 전부 "暖○"), **서로 다른 한자인데 발음(한글 표기)이 완전히 같은 후보가 중복 등장**하는 문제가 나왔다(예: 塌刃/塔刃 둘 다 "탑인").
- **결정**: 점수 정렬 뒤, 다양성 제약을 강제하는 별도 선택 단계(`selectDiverseCandidates`, `lib/naming/candidates.ts`)를 거친다.
  - **한글 발음(hangul) 중복 금지는 항상 강제**한다 — 한자가 달라도 소리 내어 읽었을 때 같은 이름이면 후보 목록에 하나만 남긴다. 완화 단계와 무관하게 항상 적용.
  - 이름 첫 글자·끝 글자 각각의 재사용 횟수, 그리고 동일 4격(수리) 조합의 재사용 개수에 상한을 둔다. 상한은 `lib/naming/config.ts`의 `CANDIDATE_DIVERSITY.stages`에 완화 단계로 정의되어 있다 — 엄격한 단계부터 시도해 요청된 개수(`candidateCount`, 3.6.1 참고)를 채우면 그 단계를 채택하고, 후보 풀이 작아 못 채우면 다음 완화 단계로 넘어간다(최종 단계는 발음 중복 금지만 남긴다).
  - 점수 동점자의 2차 정렬 키로 "이름 두 글자 원획 합이 작은 쪽"을 앞세운다. 이는 뜻 판단이 아니라 순수 수치 비교이며, 임의의 DB 삽입 순서에 의존하던 기존 동작보다는 결정 근거가 있는 정렬이다.
  - 재현·검증: `scripts/debug/repro-candidates.ts`(LLM 미호출, DB 읽기 전용)로 실제 DB 대상 재현. 회귀 테스트는 `lib/naming/candidates.test.ts`의 `selectDiverseCandidates` describe 블록과 `app/lib/match-report.test.ts`(같은 근본 원인 때문에 프론트에서 "3순위를 클릭했는데 2순위 해설이 나온다"는 증상으로도 나타났던 것을 함께 고정).
- **한자 친숙도(별도 문제, 같은 날 해결)**: 위 다양성 수정은 "같은 이름 중복"·"한 글자 반복"은 해결하지만, "刃(칼날 인)·塌(무너질 탑)처럼 실제 작명에 거의 쓰이지 않는 생소한 한자가 선택된다"는 것은 별개 원인(점수 체계에 한자의 실사용 빈도/친숙도 차원이 아예 없었음)이라 추가로 손봤다. **결정**: Unihan `kKoreanEducationHanja`(대한민국 교육부 공식 "한문교육용 기초한자" 1,800자) 포함 여부를 `hanja.is_common`(`isCommon`)으로 추가해 스코어링 가중치(`CANDIDATE_SCORE_WEIGHTS.commonHanja`)로 반영했다. 이 필드는 한자의 뜻을 판단하지 않는다 — Unihan의 사실 플래그를 그대로 옮기는 조회이며(4.1에서 이미 8자 유지 근거로 쓴 것과 동일 성격), 2.1·2.3의 "LLM은 뜻/오행을 판정하지 않는다"와 충돌하지 않는다. 로컬 시뮬레이션(DB 미변경, `scripts/debug/repro-candidates-common.ts`로 검증 후 삭제)과 실제 마이그레이션 후 재현(`scripts/debug/repro-candidates.ts`) 모두에서 여러 성씨·사주 조합에 대해 여전히 5개 다양한 후보가 나오면서 체감 친숙도가 개선됨을 확인했다(예: 刃/塌/汪/冱 계열 → 沈/州/浦/沙/李 계열). 4.1에 `is_common` 컬럼 반영.
  - **가중치 재조정 (2026.7.26)**: 처음엔 `commonHanja: 3`을 `wonhaengMatch: 2`보다 크게 잡아 친숙도가 자원오행보다 항상 우선하게 했었다. 그런데 실사용 중 "李垂頻(이수빈)"처럼 두 글자 중 하나만 용신 일치이고 나머지는 자원오행 자체가 없는(부수가 18개짜리 배속표 밖) 흔한 한자가 선택되는 사례가 나왔고, DB 조사 결과 **"빈" 음절로 읽는 한자 31개 전부 자원오행이 NULL**이라 이 사례에서는 애초에 `wonhaengMatch`가 개입할 여지가 없었다(자원오행이 있는 한자가 하나도 없어서). 이 조사를 계기로 "용신 보완(자원오행)이 친숙도에 밀려 사실상 뒷전이 된다"는 문제가 재확인돼, `wonhaengMatch`를 `commonHanja`와 같은 3으로 올렸다(2/3/1 → **3/3/1**). 두 글자 다 자원오행 일치(6점)와 두 글자 다 흔한 한자(6점)가 동률이 되어 어느 한쪽이 항상 이기지 않으며, 동률 이후는 `buildCandidates`의 2차 정렬 키(실사용 빈도 → 원획 합)로 넘어간다.
- **한계**: `is_common`은 "교육용으로 흔히 접하는 한자인가"만 나타내고, 뜻이 이름에 부적합한지(예: 어감이 무겁거나 부정적인 한자)는 여전히 판단하지 않는다 — 이는 3.4 "불용문자" 필터(사용자 제공 실무 자료 89자, 큐레이션 기반)의 영역이며, 뜻 기반 판단을 코드/LLM이 자동으로 확장하지 않는다는 2.3·3.5 원칙과 같은 이유로 의도적으로 손대지 않았다. `is_common`이면서 불용문자 목록에 없는 한자라도 어감이 무거울 수 있다(예: 壞, 霧) — 이 목록은 "여기 없다고 문제없음을 보증하지 않는다"는 3.4의 경고가 그대로 적용된다. **이 한계는 3.7에서 더 근본적으로 해결됐다** — 자유 조합 자체를 그만두고 실사용 이름 표로 후보 풀을 바꿨기 때문에, "이름 전체"가 낯설 위험은 3.7의 방식이 3.6의 `is_common`보다 훨씬 강하게 억제한다(다만 3.6의 `is_common`/자원오행 가중치는 3.7 안에서도 "같은 이름의 여러 한자 표기 중 어느 걸 고를지"를 정하는 데 계속 쓰인다).

### 3.6.1 후보 개수 선택 · 순위 폐지 · 유사 이름 소프트 필터 (Phase 8 확정, 2026.7.27)

- **배경**: 기존엔 `CANDIDATE_COUNT=5` 고정값이었고, 화면은 LLM이 매긴 `rank`로 후보를 재정렬해 "1순위" 큰 타일 + 작은 타일 벤토 그리드로 보여줬다(3.1 이전 버전). 실사용 피드백에서 두 가지 문제가 나왔다: (1) 5개 고정이라 더 많은 후보를 보고 직접 고르고 싶은 사용자를 만족시키지 못했고, (2) 순위·타일 크기 차이가 "1순위가 정답"이라는 인상을 줘 특정 후보에 선택이 쏠리는 위치 편향(primacy effect)이 있었다. 여기에 더해 (3) 후보 다양성 로직(3.6)이 글자 재사용은 제한해도 "규리/규린/규나"처럼 글자는 다르지만 소리가 거의 같은 이름이 한 결과에 함께 나오는 것은 걸러내지 못했다.
- **결정 1 — 후보 개수 선택**: `lib/naming/config.ts`의 `CANDIDATE_COUNT_OPTIONS = [3, 5, 10]`(및 기본값 `DEFAULT_CANDIDATE_COUNT = 5`)로 대체했다. `buildCandidates`가 `candidateCount`를 입력으로 받고(`BuildCandidatesInput.candidateCount`), `/api/name` 요청 바디의 `candidateCount` 필드(생략 시 기본값)로 전달된다. 입력 폼(`InputForm.tsx`)에 3/5/10 선택 컨트롤을 추가했다.
- **결정 2 — 순위 폐지, 강점 배지로 대체**: LLM 응답(`NamingReport.candidates[]`)에서 `rank` 필드를 완전히 제거했고, 프롬프트에서도 "순위를 매겨라" 지시를 없애고 대신 "모든 후보를 동등한 비중으로, 우열 비교 표현 없이 설명하라"고 명시했다. 화면(`ResultsDashboard.tsx`)은 `report.rank`로 재정렬하지 않고 API가 반환한 순서를 그대로 쓰며, 벤토 타일도 동일 크기로 통일했다(`CandidateTile.tsx`, "1순위 큰 타일" 위계 폐지 — design.md 3.1도 함께 갱신). 대신 순위가 없어도 각 후보가 왜 좋은지 알 수 있도록, `candidates.ts`가 결정적으로 계산하는 강점 태그(`Candidate.highlights: CandidateHighlight[]`)를 신설했다 — 발음오행 상생·수리사격 4격 길격(모든 후보 공통) + 자원오행 용신 일치 개수·교육용 기초한자 여부·음양 균형·실사용 빈도 상위권(후보마다 다름) 중 해당하는 항목을 담는다. 전부 이미 계산된 사실의 조회·집계일 뿐 LLM이나 새 규칙이 만든 판단이 아니므로 2.1·2.3과 충돌하지 않는다. 프론트는 `HighlightBadges.tsx`(타일에 축약 표시)·`CandidateDetail.tsx`("이 이름의 강점" 목록)로 노출한다.
  - **표시 순서 무작위화**: 점수순으로 정렬된 내부 계산 결과를 그대로 화면 순서로 쓰면 위치 자체가 암묵적 순위가 되므로, `app/api/name/route.ts`가 응답 직전 `app/lib/shuffle.ts`의 `shuffleArray`로 순서를 섞는다. 이건 계산 로직이 아니라 프레젠테이션 관심사라 `lib/naming/` 밖(`app/lib/`)에 둔다 — `buildCandidates` 자체는 여전히 점수순 결정적 배열을 반환해 테스트 가능성을 유지한다.
- **결정 3 — 유사 이름 소프트 필터**: "규리/규린/규나"처럼 자모 하나 차이로 거의 같게 들리는 이름이 한 결과에 몰리는 것을 막기 위해 `lib/naming/similarity.ts`를 신설했다. 완성형 한글 음절을 유니코드 규칙으로 초성·중성·종성 인덱스로 분해해(`decomposeSyllable`), 이름 두 글자(고정 길이) 사이의 자모 불일치 개수를 세는 `hangulJamoMismatchCount`와, 그 값이 임계값(`config.ts SIMILAR_NAME_MAX_JAMO_MISMATCH = 1`) 이하면 유사로 보는 `isSimilarName`으로 구성된다 — 뜻·오행 판단이 아니라 기계적 자모 비교라 2.1과 충돌하지 않는다.
  - `CANDIDATE_DIVERSITY.stages`에 `avoidSimilar: boolean`을 추가해(앞 3단계 `true`, 마지막 완화 단계만 `false`) `candidates.ts`의 `pickWithLimits`가 이미 뽑힌 후보와 유사한 후보를 이 단계에서 건너뛰게 했다. **완전 차단이 아니다** — 후보 풀이 작아 다른 다양성 제약과 마찬가지로 채워야 할 개수를 못 채우면 마지막 완화 단계에서 유사한 이름도 포함될 수 있다("최대한 거르되 무조건 막지는 않는다"는 요청 그대로).
  - 회귀 테스트: `lib/naming/similarity.test.ts`(자모 불일치 계산 자체), `lib/naming/candidates.test.ts`의 `selectDiverseCandidates` describe 블록에 "규리/유리"(첫 글자가 달라도 자모 유사도로 걸러지는 사례) 케이스 추가.
- **6장 미결정 사항 갱신**: "다중 후보 제시 시 후보 개수 및 정렬 기준" 항목은 이 결정으로 해결됐다(개수는 3/5/10 선택, 정렬은 폐지하고 강점 배지로 대체).

### 3.6.2 강점 배지 소재를 결정적 태그에서 한자 뜻 기반 LLM 서술로 전환 (2026.7.27)

- **문제**: 3.6.1에서 순위 대신 도입한 `Candidate.highlights`(강점 태그)는 `wonhaeng`(자원오행 용신 일치 0/1/2)·`common`(친숙한 한자)·`yinYang`(음양 균형)·`frequency`(실사용 빈도 상위권) 4가지 불리언 차원의 조합일 뿐이었다. 같은 배치 안의 후보들은 대부분 이미 비슷한 조건(하드 필터 통과 + 유사한 점수대)을 만족하므로 실사용에서 "추천된 이름들의 장점이 서로 거의 똑같이 보인다"는 피드백을 받았다 — 조합 가짓수 자체가 적어 구조적으로 차별화가 어려웠다.
- **결정**: 각 후보 타일·상세 화면의 "이 이름의 강점" 표시를 코드가 계산한 고정 태그 대신 `explainCandidates`(LLM, `lib/llm/explain.ts`)가 생성하는 `strengths: string[]`(후보마다 정확히 3개, 6~18자 내외의 짧은 구절)로 바꿨다. 이 문구는 그 후보 한자 각각의 고유한 뜻(이미 DB에서 조회된 `meaning`)과 그 뜻이 이 사주·용신과 만나는 지점을 재료로 삼고, 프롬프트로 다음을 강제한다: (1) 이번 배치의 모든 후보가 공통으로 가진 사실(발음오행 상생·4격 전부 길격 — 이미 다른 섹션에 항상 표시됨)은 다시 쓰지 않는다, (2) 같은 배치의 다른 후보들과 소재·형용사가 겹치지 않게 한다, (3) 실사용 빈도·인기·"몇 명이 쓰는 이름인지"는 절대 언급하지 않는다(사용자가 명시적으로 제거를 요청함 — 등수/인기 프레이밍이 아니라 그 이름 고유의 의미로 강점을 설명해야 한다는 취지).
- **2.3과의 정합성**: 이는 LLM이 새로운 오행·용신·길흉 판정을 만드는 것이 아니다 — 한자 뜻(코드가 DB에서 조회한 사실)과 이미 확정된 사주·용신을 근거로, 어느 지점을 부각할지 고르고 서술하는 것뿐이다(2.3이 이미 허용한 "후보 간 미묘한 어감·인상을 비교" 역할의 연장). `Candidate.highlights`(결정적 계산, `candidates.ts`)는 폐기하지 않았다 — 여전히 계산되어 LLM 프롬프트의 grounding 근거(이 후보가 실제로 자원오행 일치·친숙 한자·음양 균형을 만족하는지)로 전달되지만, 화면에 직접 렌더링하지는 않는다. `frequency` 태그와 후보별 실사용 빈도 숫자는 이제 LLM 프롬프트 컨텍스트 자체에서도 제외했다(예전엔 grounding 겸 해설 소재로 전달했었다, 3.7 참고) — 언급 금지를 프롬프트 지시뿐 아니라 데이터 노출 자체로 이중 차단한 것이다.
- **UI**: `HighlightBadges.tsx`는 `CandidateHighlight[]`가 아니라 `strengths: string[]`를 받는 형태로 단순화됐고, `CandidateDetail.tsx`의 "이 이름의 강점" 체크리스트도 같은 `strengths`를 쓴다. `design.md` 3.1도 함께 갱신.

### 3.6.3 자원오행 점수 재설계 — 이진 매치에서 "주/보조 용신 차등 + 오행분포 부족량 비례"로 (2026.8.1)

- **문제(사용자 실사용 확인)**: 같은 성씨로 생년월일시를 완전히 다르게 입력해도 상위 후보 이름이 반복 등장한다는 의심이 있어 실제로 재현했다. 원인은 두 가지가 겹친 구조적 문제였다: (1) 발음오행 하드 필터(`requiredPhoneticElements`)와 4격 하드 필터는 성씨(초성 오행·원획)에만 의존하고 사주(용신)와 무관해서, 같은 성씨면 "통과 가능한 이름 풀" 자체가 사주와 상관없이 항상 동일하다(예: 김씨는 항상 정확히 같은 150개/133개 통과). (2) 그 풀 안에서 사주가 관여하는 유일한 점수 축인 자원오행 매치(`wonhaengMatch`)가 "용신 2개 중 일치 여부"라는 이진값(0/1/2)뿐이었는데, `hanja.element`가 33%나 NULL이라(3.5) 동점이 매우 흔했다. 동점자는 실사용 빈도 → 원획 합 순으로 갈리는데 둘 다 사주와 무관해서, 결과적으로 사주가 달라도 상위 5개가 거의 동일하게 나왔다(1993/1975/2001년생 김씨 남으로 실측 — "리안"이 3개 사주 모두의 top5에 등장).
- **결정**: 자원오행 점수를 연속값으로 재설계했다. 새 판정을 만드는 게 아니라 이미 확정된 두 값(용신 도출 순서, 오행분포 수치)을 더 정밀하게 조합하는 것뿐이라 2.1과 충돌하지 않는다.
  - **주용신/보조용신 차등**: `yongsin[0]`(신약이면 인성, 신강이면 식상 — 억부이론상 더 직접적인 보완 대상, `yongsin.ts`)과 `yongsin[1]`(신약이면 비겁, 신강이면 관성 — 보조적 대상)을 각각 가중치 2/1로 차등한다(`config.ts CANDIDATE_SCORE_WEIGHTS.wonhaengPrimaryWeight/wonhaengSecondaryWeight`).
  - **오행분포 부족량 비례**: 같은 "용신 일치"라도 오행분포(`ElementDistribution`, `aggregateElements` 이미 계산된 값)에서 평균(`ELEMENT_DISTRIBUTION_TOTAL/5`=1.6)보다 얼마나 부족한지를 0(평균 이상)~1(전혀 없음) 비율로 추가 반영한다.
  - 두 축을 결합한 `wonhaengScore()`(`candidates.ts`)가 `bestRealization`의 점수 계산에서 기존 `wonhaengMatches * weight` 항을 대체한다. 만점은 기존 이진 방식의 만점(6점, `commonHanja` 만점과 동률)과 정확히 맞췄다: 한 글자가 주용신을 완전히 부족한 상태로 채우면 2×(1+1)=4, 다른 한 글자가 보조용신을 완전히 부족한 상태로 채우면 1×(1+1)=2, 합 6 — 2026.7.26 "동률" 재조정 결정(3.6)을 그대로 유지한다.
  - `BuildCandidatesInput`에 `distribution: ElementDistribution` 필드가 추가돼 `app/api/name/route.ts`가 이미 계산해 둔 `elementDistribution`을 그대로 전달한다.
- **효과 재현(실측)**: 재설계 후 같은 3개 사주(1993/1975/2001년생 김씨 남)로 재실행 — 신약/木火 용신인 두 사주(1993, 2001)는 둘 다 주용신이 木으로 같아 top5가 겹치지만(같은 핵심 보완 오행을 요구하니 비슷한 추천이 나오는 것은 의도된 개인화이지 결함이 아니다), 주용신이 다른 사주(1975, 土金)는 top5가 뚜렷하게 달라짐을 확인했다. 즉 이제 "성씨가 같아서" 반복되는 게 아니라 "주용신(사주의 핵심 요구)이 같아서" 겹치는 것으로 원인이 좁혀졌다.
- **한계 — 여전히 남는 것**: 발음오행/4격 하드 필터의 "성씨당 고정 통과 풀"(150개 안팎) 자체는 이번 변경으로 줄지 않았다 — 사주와 무관하게 발음오행이 성씨 초성만으로 정해지는 것은 3.1의 정통 이론 그대로이므로 하드 필터 자체를 바꾸지 않았다. 풀 크기를 늘리려면 `given_name` 데이터 확장이 필요하다(3.7, 6장 미결정 사항 "이름 후보 풀 확장" 참고 — 이번 변경으로 해결되지 않았다).
- **회귀 테스트**: `lib/naming/candidates.test.ts` — 기존 이진 매치 테스트를 새 연속 점수 기준으로 갱신했고, 주/보조 차등과 부족량 비례가 상호작용해 "완전히 부족한 보조용신 매치"가 "부족하지 않은 주용신 매치"와 동점을 이루는 경계 케이스를 새로 추가했다. tsc·전체 vitest(82개) 통과 확인.
- **6장 미결정 사항 갱신**: "후보 순위 알고리즘의 가중치(수리 vs 발음 vs 자원오행 vs 친숙도)" 항목 중 자원오행 축의 이진성 문제는 이 결정으로 해결됐다. 다만 수리(81수리)·발음오행은 여전히 하드 필터(소프트 스코어화 안 됨)라 그 부분은 미결정으로 남는다.

### 3.6.4 후보 생성 하드 필터를 score.ts 등급 기준(총점 90점)으로 재설계 (Phase 11 확정, 2026.8.1)

- **문제**: 3.6.3에서 "같은 성씨면 사주가 달라도 상위 후보가 반복된다"는 문제를 자원오행 점수 재설계로 완화했지만, 근본 원인 중 하나인 "성씨당 고정 통과 풀"(3.6.3의 "한계" 항목)은 그대로 남아 있었다. 실측 결과 그 풀이 성씨마다 극단적으로 차이 났다 — 김씨(木초성) 남성 풀은 150개인 반면 박씨(水초성) 남성 풀은 단 **7개**였다. 원인을 score.ts(3.10, 무료 채점 서비스)의 등급 체계로 역산해보니, 당시 하드 필터("발음오행 순방향 상생 전부 + 4격 전부 길수")가 사실상 **총점 95점 이상**을 요구하는 것과 수학적으로 동치였다 — score.ts 자신이 이미 정의해둔 "상극만 나쁘고 비화·역상생은 절반짜리, 반길은 길의 절반"이라는 4단계 등급 체계를 생성 엔진은 전혀 쓰지 않고 있었던 것이다.
- **성씨 초성별 조사**: 전체 성씨×성별 204개 조합을 스윕한 결과, 발음오행 요구 패턴이 5개 그룹(성씨 초성 오행별)으로 나뉘는데 그중 두 그룹(水초성: 박·문·민·백·마·모·명·배·변·방·반·편·표 / 金초성: 서·석·선·설·소·신·심 등)이 구조적으로 극단적으로 좁은 풀(水 M 8개·金 F 8개)을 가진다는 걸 확인했다 — 우연이 아니라 해당 발음오행 요구 패턴에 맞는 한글 이름 자체가 `given_name` 큐레이션 표에 적기 때문이다. 이 그룹엔 한국에서 매우 흔한 성씨(박=3위권)가 포함돼 실사용 영향이 크다.
- **결정**: `candidates.ts`의 이중 하드 필터(발음오행 순방향 상생 전부 + 4격 전부 길수)를 폐기하고, `score.ts`의 `scoreName()` 총점이 `config.ts CANDIDATE_MIN_TOTAL_SCORE`(90점, score.ts의 S등급 임계값과 동일) 이상인지 하나로 대체했다. 새 이론을 만드는 게 아니라 score.ts가 이미 확정한 등급 체계(3.10)를 생성 엔진에도 일관되게 적용하는 것뿐이다(2.1). 90점이라는 값은 임의로 고른 게 아니라 수학적으로 "발음오행 링크 2개(성→음절1→음절2) 중 상극은 절대 없고, 비화·역상생은 최대 1개까지만 허용"이라는 경계와 정확히 일치한다 — 상극 링크가 하나라도 있으면(0점) 나머지(수리40·자원20·음양10)가 전부 만점이어도 85점을 못 넘는다(발음15+수리40+자원20+음양10=85 < 90).
- **실측 효과**: `given_name` 풀 전체(1,448개)를 대상으로 "이 이름이 낼 수 있는 최고 점수"를 시뮬레이션한 결과(2026.8.1) 박씨(M) 통과 풀이 **7개 → 106개(15배)**, 김씨(M)는 **150개 → 214개(1.4배)**로 늘었다. 90점 이상으로 새로 들어온 이름(예: 박씨의 성민·정민·수민·승빈)은 전부 실제로 흔히 쓰이는 자연스러운 이름이며, 제외됐던 이유는 순전히 "성→이름 첫 글자" 링크가 역상생(예: 水성→金이름, 금생수의 역방향)이었기 때문이지 이름 자체의 문제가 아니었다.
- **구현**:
  - `bestRealization()`(`candidates.ts`)이 4격 하드 필터(`isAuspiciousNumber`) 대신 `scoreName()`을 호출해 총점을 게이트로 쓴다. 게이트를 통과한 조합들 사이의 순위는 여전히 3.6.3의 내부 가중치(자원오행 주/보조 차등·부족량 비례 + 친숙한 한자 + 음양균형)로 매긴다 — score.ts의 `scoreWonhaeng`은 이진 매치라 3.6.3만큼의 순위 결정력이 없기 때문이다. 즉 **게이트는 score.ts와 공유하고, 순위는 candidates.ts 고유 로직을 유지**하는 2단계 구조다.
  - `buildCandidates()`의 이름 순회 루프에서 발음오행 순방향 상생 사전 필터(`matchesRequired`)를 제거했다 — 이제 어떤 발음오행 조합의 이름이든 일단 평가 대상이 되고, 최종 게이트(총점 90+)만 통과하면 후보가 될 수 있다. `Candidate.phoneticElements`도 더 이상 모든 후보가 공유하는 "이상적" 체인이 아니라, 그 후보의 **실제** 발음오행 체인(성 + 실제 두 음절 오행)을 담도록 바꿨다 — LLM 프롬프트(`explain.ts`)와 상세 화면(`CandidateDetail.tsx`)에 실제 흐름이 정확히 표시된다.
  - **성능 최적화(필수)**: 사전 필터 제거로 모든 이름(최대 1,448개)이 한자 조합 전수탐색(및 매 조합 `scoreName` 호출)에 들어가면서 `buildCandidates()` 단일 호출이 5~15초까지 느려지는 회귀가 있었다. 발음오행 점수는 한자 선택과 무관하게 이름(성+음절)만으로 정해진다는 점을 이용해, 그 값만으로 "어떤 한자를 골라도 총점 90에 도달할 수 없는" 이름(발음오행 링크 2개 중 22.5점 미만 — 비화/역상생이 2개거나 상극이 하나라도 있는 경우)을 한자 탐색 전에 미리 제외하는 사전 가지치기(`MIN_PHONETIC_POINTS_FOR_GATE = 22.5`)를 추가했다. 최적화 후 실측: 200ms~2.8초/호출(성씨별 편차— 통과 폭이 넓은 성씨일수록 한자 조합 탐색량이 많아 다소 느림). 정확도 손실 없는 가지치기다(90점 도달이 수학적으로 불가능한 경우만 제외).
  - `buildHighlights()`의 "발음오행 상생"·"수리사격 4격 길격" 배지도 더 이상 모든 통과 후보가 공통으로 만족하는 사실이 아니므로(비화/역상생·반길이 섞인 후보도 통과 가능), 실제로 참일 때만 조건부로 표시하도록 바꿨다(2.1 — 없는 사실을 있다고 표시하지 않음).
  - `requiredPhoneticElements()`는 하드 필터로 더 이상 쓰이지 않지만, "이상적 순방향 상생 체인" 자체를 계산하는 유틸리티로서 테스트·문서적 참조용으로 남겨뒀다.
- **회귀 테스트**: `lib/naming/candidates.test.ts` — 기존 이진 하드필터 가정으로 짜인 테스트(예: `yongsin: []`로 "자원오행 무시"를 흉내내던 관행)가 새 게이트 아래에서는 총점을 왜곡시켜(자원오행 20점을 통째로 잃음) 실패했다. 실제 사주에서는 `yongsin`이 항상 길이 2인 배열이므로, 테스트를 현실적인 `yongsin` 값으로 고쳐 새 게이트 하의 실제 동작을 검증하도록 갱신했다. tsc·전체 vitest(82개)·eslint 통과, 실제 DB로 204개(성씨×성별) 스윕 재확인.
- **한계 — 여전히 남는 것**: 이 변경은 "통과 풀을 표 안에서 최대한 넓히는" 것이지 표 자체를 넓히는 게 아니다 — `given_name` 큐레이션 표(성별당 1,400여 개)에 아예 없는 이름은 여전히 후보가 될 수 없다(3.7). 표 자체를 3,000개 규모로 확장하는 작업은 사용자가 별도로 진행 중이며, 확장되면 이번 변경(통과 비율 자체를 높임)과 곱해져 효과가 배가된다.
- **6장 미결정 사항 갱신**: "후보 순위 알고리즘의 가중치" 항목 중 "수리(81수리)·발음오행이 하드 필터라 소프트 스코어화 안 됨" 문제가 이 결정으로 해결됐다 — 이제 이 둘도 score.ts를 통해 사실상 소프트 스코어(등급별 배점)로 반영된다. "이름 후보 풀 확장" 항목은 여전히 미결정(사용자가 진행 중)이다.

### 3.6.5 최종 선택을 점수순 고정 상위 대신 "상위 비율 내 무작위"로 전환 (Phase 12 확정, 2026.8.1)

- **문제(실측)**: 3.6.4로 게이트 통과 풀을 넓혔지만, 최종 후보는 여전히 내부 순위 점수(3.6.3)의 최상위를 그대로 뽑았다. 무작위 사주 200개(김씨 M)로 모든 쌍(19,900쌍)을 비교한 결과, **주용신(yongsin[0])이 우연히 같은 쌍(전체의 약 20.8%)은 72.7%가 top5 중 4개 이상이 겹쳤다** — 다르면 0%였다. 원인은 내부 순위 점수가 사실상 이산적(주용신 일치 여부가 지배적 항)이라 같은 주용신을 가진 사람들 사이에서는 동점자가 매우 흔하고, 그 동점자는 실사용 빈도(고정값)로 갈리기 때문에 "누가 1등인지"가 사주와 거의 무관하게 고정되는 구조였다.
- **결정**: 최종 `candidateCount`개를 뽑을 때, 점수순 최상위를 그대로 쓰지 않고 **게이트 통과 개수에 따른 상위 비율 구간 안에서 무작위로** 뽑는다(`config.ts RANDOM_SELECTION_POOL`): 통과 개수가 **200 미만이면 상위 30%**, **200 이상이면 상위 20%**(둘 다 `candidateCount` 미만으로 좁아지지 않도록 최솟값 보정) 안에서 순서를 섞은 뒤, 기존 다양성 선택(3.6, `selectDiverseCandidates`)을 그대로 적용한다. 상위 비율 밖은 무작위화하지 않고 점수순 그대로 이어붙여, 비율만으로 개수를 못 채우는 작은 풀에서도 최대한 채운다. 풀이 클수록(200+) 비율을 더 좁힌 이유: 풀이 클 때 상위 20%도 이미 충분히 많은 절대 개수이고, 비율을 30%로 유지하면 품질 편차가 큰 후보까지 섞일 수 있어서다.
- **테스트 가능성(CLAUDE.md 8.4)**: `Math.random()`을 직접 쓰면 `buildCandidates`가 더 이상 순수 함수가 아니게 돼 테스트로 결과를 고정할 수 없다. `BuildCandidatesInput.random?: () => number`(생략 시 `Math.random`)으로 난수 생성기를 주입받게 해, 테스트는 고정 시퀀스(예: 항상 자기 인덱스로 스왑되는 값 → 사실상 셔플 없음)를 넣어 결정적으로 검증하고, 실사용은 기본값(`Math.random`)을 그대로 쓴다. `buildRandomizedSelectionPool()`을 `selectDiverseCandidates`처럼 별도 export해 "섞이는 범위(상위 topCount)는 항상 원래 순위 집합과 같다"는, random 값과 무관하게 항상 성립해야 하는 구조적 성질을 직접 단위테스트했다(`candidates.test.ts`).
- **회귀 테스트**: `lib/naming/candidates.test.ts`에 `buildRandomizedSelectionPool` 전용 describe 추가(200 미만/이상 임계값 분기, NO_SHUFFLE 시 항등, 실제 셔플 시 순서 변경, 작은 풀에서 candidateCount 하한 보정). 기존 `buildCandidates` 테스트들은 원래 점수 로직만 검증하려는 의도였으므로 전부 `random: NO_SHUFFLE`을 주입해 무작위화와 무관하게 결정적으로 유지했다(그렇지 않으면 `Math.random` 기본값 때문에 실행마다 결과가 달라져 flaky해진다). tsc·전체 vitest(87개)·eslint 통과.
- **한계**: 이 변경은 "같은 주용신 안에서의 다양성"만 늘린다 — 애초에 주용신 자체가 5가지뿐이라 우연히 같을 확률(약 20%)은 줄지 않는다. 더 줄이려면 이름 풀 확장(3.7, 사용자 진행 중)이나 오행분포 세부값의 영향력 강화가 필요한데, 후자는 "주용신이 같으면 비슷한 보완이 맞다"는 억부용신 이론(3.3) 자체와 긴장 관계가 있어 신중한 판단이 필요하다(6장 참고).

### 3.6.6 게이트 통과 이름 중 반대 성별 쪽이 압도적으로 우세한 이름 제외 (Phase 13 확정, 2026.8.2)

- **문제**: 3.6.4 게이트(총점 90점)를 통과한 이름 중에도 "도연"·"지안"처럼 실제로는 반대 성별 이름으로 훨씬 흔히 쓰이는 이름이 섞여 나올 수 있다 — 발음오행·수리·자원오행·음양은 모두 사주·한자 기준일 뿐 "이 이름이 실사용에서 어느 성별 인상이 강한가"를 전혀 반영하지 않기 때문이다. `given_name` 테이블은 남녀 두 개의 독립된 실사용 빈도(3.7.1)를 이미 갖고 있으므로, 이를 교차 비교해 걸러낼 수 있다.
- **결정**: 게이트를 통과한 이름이라도, 검색한 성별의 `given_name.frequency`(자기 성별 빈도)보다 **반대 성별 쪽 빈도가 `CROSS_GENDER_FREQUENCY_RATIO`(`config.ts`, 기본 2)배 이상**이면 후보에서 제외한다. 반대 성별 쪽에 그 이름이 아예 없으면(두 성별 풀이 독립적으로 큐레이션돼 겹치지 않는 이름도 흔함, 3.7.1) 비교 대상이 아니므로 제외하지 않는다. 순수 수치 비교이며 뜻 판단이 아니므로 2.1과 충돌하지 않는다.
- **검증(실측, `scripts/debug/cross-gender-filter-check.ts`, 진단 전용 스크립트로 남겨둠)**: 성씨 4개(김·이·박·서, 초성 발음오행이 겹치지 않게 木/木/水/金 선택) × 사주 3개 × 성별 2종 = 24개 조합으로 게이트 통과 풀에 이 규칙을 적용했다. 전체 게이트통과 13,276건 중 1,815건(13.7%) 제거, 잔여 11,461건. 가장 좁은 케이스(박씨 F)도 게이트통과 112~113개 중 24개 제거 후 88~89개가 남아, 후보 개수 최대 옵션(10개, 3.6.1)보다 8배 이상 여유가 있어 특정 성씨·사주 조합에서 후보가 부족해지는 문제는 없음을 확인했다. 제거된 이름들(도연·다율·로아·시온·준희·규빈 등)은 실제로 반대 성별 인상이 강한 이름들로, 직관과 일치했다. 사용자가 예시로 든 "단영"도 실측 DB에서 M=10/F=20으로 정확히 2배 경계에 해당해 규칙대로 걸러짐을 확인했다.
- **구현**: `isCrossGenderSkewed(ownFrequency, oppositeFrequency)`(`candidates.ts`)가 판정 함수다. `BuildCandidatesInput.oppositeGenderFrequency?: Map<string, number>`(생략 시 빈 Map — 필터 미적용, `random`과 동일한 옵셔널+안전한 기본값 패턴)로 반대 성별 빈도를 주입받아, 이름 순회 루프 맨 앞(한자 탐색 이전)에서 걸러 불필요한 계산을 피한다. `app/api/name/route.ts`가 `getGivenNamesByGender(oppositeGender)`를 추가로 조회해 `hangul → frequency` 맵으로 전달한다 — 사주 계산에는 영향 없다(3.7의 "성별은 이름 후보 풀 선택에만 쓰인다" 원칙 유지, 이번엔 반대쪽 풀도 참조용으로만 쓰임).
- **한계**: `given_name.frequency`는 두 출처(기존 `korean_name.xlsx` 상위 300 + 신규 `namechart.kr`)가 섞여 스케일이 다를 수 있다는 기존 한계(3.7.1)가 이 비교에도 그대로 적용된다 — 다만 비교 대상 대부분이 최근 신규 풀(같은 스케일)이라 실질적 왜곡은 크지 않은 것으로 판단했다. 배수(2배) 임계값 자체는 사용자 지시값을 그대로 채택한 것으로, 실사용 피드백에 따라 재조정될 수 있다.
- **회귀 테스트**: `lib/naming/candidates.test.ts` — `isCrossGenderSkewed` 전용 describe(경계값 2배 포함) + `buildCandidates`에 반대 성별 빈도 필터 3종(정확히 2배 제외·2배 미만 미제외·미전달 시 미적용) 추가. tsc·전체 vitest(95개)·eslint 통과.

### 3.7 이름(한글) 후보 풀 — 실사용 이름 표 기반으로 전환 (Phase 6 확정, 2026.7.26)

- **배경**: 3.6의 `is_common` 가중치로도 여전히 실제로는 거의 쓰이지 않는 한자·이름이 후보에 나올 수 있었다(발음오행·수리·자원오행 조건만 만족하면 되는 자유 조합이라, "존재는 하지만 아무도 안 쓰는 이름"을 만들어낼 구조적 위험이 남아 있었다). 사용자가 대한민국 출생 등록 통계 기반의 실사용 이름 표(`etc/korean_name.xlsx`, 성별별 시트, 이름 A열 + 사용빈도 B열, 시트당 원본 3,600행)를 제공했다.
- **결정**: 이름(hangul) 후보를 더 이상 한자 자유 조합으로 만들지 않는다. **이 표의 A열에 있는 이름만 후보로 나올 수 있다.** `buildCandidates`는 이제 (성 오행 → 발음오행 하드 필터를 만드는 것까지는 기존과 동일하지만) 한자 조합을 먼저 만들고 나서 이름을 조립하는 대신, **큐레이션된 이름 목록을 먼저 순회**하고 각 이름이 발음오행 하드 필터를 통과하는지 확인한 뒤, 통과한 이름에 대해서만 그 두 음절을 표기할 수 있는 실제 한자 조합(4격 하드 필터 + 기존 점수 체계로 최고점 하나)을 찾는다. 같은 이름에 대해 한자 조합이 여러 개 가능해도(예: "민"=民/旻/珉…) 최고점 하나만 그 이름의 대표 후보로 남긴다 — 그래야 같은 이름이 중복 등장하지 않는다.
- **데이터 파이프라인**: `etc/korean_name.xlsx`(여자이름/남자이름 시트) → `etc/parse_korean_names.py`(dedupe + 2글자만 필터링 → `scripts/db/data/given_names.json`) → `scripts/db/seed-given-names.js` → Turso `given_name` 테이블(4.4 참고). 원본 시트는 정확히 동일한 (이름,빈도) 행이 1~4회 반복돼 있었다(원본 편집 과정의 산물로 추정, 서로 다른 연도 스냅샷이 아님을 값 비교로 확인) — 이름 기준 dedupe로 성별당 1,500개 고유 이름을 얻었고, 그중 2글자만(`GIVEN_NAME_LENGTH=2`, 3.6/Phase 4 기존 제약) 남겨 여자 1,442개·남자 1,448개를 적재했다. 1글자·3~4글자 이름은 제외했다 — 새 학파 판단이 아니라 기존에 이미 확정된 제약을 그대로 적용한 것뿐이다.
- **성별 도입**: 이름 후보 풀이 성별마다 다르므로, `/api/name` 요청에 `gender`("M"|"F")가 필수 필드로 추가됐다. 사주·용신 계산에는 영향을 주지 않는다(CLAUDE.md는 사주 계산에 성별을 쓰지 않는다) — 오직 `given_name` 테이블 조회(`db.ts getGivenNamesByGender`)에만 쓰인다. 0장에서 애초에 "생년월일시 + 성별 + 성씨" 입력을 언급했었는데, Phase 4~5까지는 엔진이 성별을 쓰지 않아 프론트 폼에서 빼놓았었다(Phase 6 초기 빌드 노트 참고) — 이제 실제로 쓰이므로 입력 폼에 되돌렸다.
- **동점 처리 갱신**: 이름 하나당 실사용 빈도(`frequency`, `given_name.frequency`)를 후보에 그대로 실어 나른다(`Candidate.frequency`). 점수 동점자는 빈도가 높은(더 많이 쓰이는) 이름을 우선하도록 정렬 키를 추가했다 — 순수 수치 비교이며 뜻 판단이 아니다. **(2026.7.27 갱신, 3.6.2 참고)**: 이 빈도값을 LLM 해설 프롬프트(`lib/llm/explain.ts`)에 전달해 해설에서 언급하게 했던 결정은 폐기했다 — 사용자 피드백에 따라 "몇 명이 쓰는 이름인지"류의 프레이밍을 결과 화면 어디에도 노출하지 않기로 했다. `frequency`는 여전히 동점자 정렬(내부 계산)에만 쓰이고, LLM 프롬프트 컨텍스트와 화면 출력 어디에도 전달되지 않는다.
- **검증**: 실제 DB로 30개 성씨 × 6개 사주 × 2개 성별 = 360개 조합 스윕(`scripts/debug/sweep-candidates.ts`) → 후보 부족(5개 미만)·발음 중복 0건. 실제 `/api/name` 호출(LLM 포함)로 종단 확인 — 도윤·서준·수빈·성민 등 실제 널리 쓰이는 이름만 후보로 나옴을 확인. 이전 자유 조합 방식 대비 속도도 더 빠르다(약 112ms/조합 → 약 10ms/조합, 큐레이션 목록 1,400여 개만 순회하므로).
- **한계**: 큐레이션 표가 상위 실사용 이름 위주라(시트당 원본 3,600행, 고유 1,500개) 매우 희귀하거나 개성적인 이름은 후보에 나올 수 없다 — 이는 의도된 트레이드오프다(3.5·3.6이 반복해 강조한 "존재하지 않는/아무도 안 쓰는 이름을 만들어내는 위험"을 원천 차단하는 대가). 더 넓은 이름을 지원하려면 사용자가 더 큰 표를 제공하거나, 다른 공식 통계 출처를 추가로 반영하는 결정이 필요하다(6장 미결정 사항 참고).

### 3.7.1 이름 후보 풀 확장·재구성 — namechart.kr 기반 + 가중치 도입 (2026.8.2)

- **배경**: 3.7의 `etc/korean_name.xlsx` 기반 풀(성별당 1,400여 개, 2015~2020년대 초 스냅샷 1개 추정)을 사용자가 직접 검토하며 "예쁘다고 느끼는 이름 위주로 더 넓히고 싶다"는 요청에 따라 교체했다. 새 원본은 `namechart.kr`(비공식, 대법원 전자가족관계등록시스템 데이터 기반)에서 2020~2026년 출생 등록 이름을 연도별로 수집(`etc/scrape_namechart.py` — koreanname.me API는 페이지가 깊어질수록 응답이 커지는 구조라 504가 잦아 폐기, namechart.kr은 페이지당 고정 50명이라 안정적이었음)해 이름별 count를 연도 합산했다.
- **필터링 단계**(전부 사용자가 직접 확정한 기준, `etc/final_name_{male,female}.csv`로 귀결):
  1. 2글자만 남김(`GIVEN_NAME_LENGTH=2`, 기존 제약과 동일)
  2. "어감 규칙" — 옛날식 끝 글자(예: 여자 자·순·숙·옥…, 남자 식·철·만·갑…)·반대 성별에 강하게 치우친 끝 글자·여자 전용 옛날식/남성적 첫 글자를 제거하되, **실사용 count>10인 이름은 규칙과 무관하게 무조건 보존**(실사용 증거 우선). count 2~10은 표준 목록만, count 0~1은 확장 목록까지 적용. 이어서 사용자가 직접 지정한 추가 시작/끝 글자 목록(카운트 예외 없이 적용)과 개별 수동 제거(대화·라감·라세) 반영.
  3. count≤3 제거.
  4. 서양식/종교성 이름 제거 — 노아·에덴·아담처럼 명백한 성경·서양 인명은 현재 대중적이어도 **원칙대로 제거**(사용자 확인). "-엘"(마이클·가브리엘·다니엘의 그 히브리어 "神" 어미) 끝 이름군과 "-든"(에덴/에이든 유래지만 지금은 독자적 한국식 트렌드) 끝 이름군도 같은 원칙으로 제거하기로 확정.
  5. 고유명사/단어 연상 이름 제거 — 서울·한강·금강·한글·이젤 등.
  - 재현 스크립트는 `etc/scrape_namechart.py`(수집)만 남아 있고, 이후 필터 단계는 이번 세션에서 인라인으로 실행되어 별도 스크립트로 저장되지 않았다 — 향후 재조정 시 이 절의 기준 설명을 참고해 다시 구현해야 한다(재현성 갭, 6장 참고).
- **최종 규모**: 남 3,077개 / 여 2,042개(전부 2글자, count≥4).
- **기존 풀과의 병합 및 가중치**: 완전 교체가 아니라, 기존 `korean_name.xlsx` 풀에서 `frequency` 상위 남녀 각 300개는 그대로 유지하고 새 namechart.kr 풀 전체를 더했다(같은 이름이 양쪽에 있으면 새 풀의 count로 덮어씀, `etc/build_final_given_names.py`). 최종 합계는 남 3,092개 / 여 2,056개(중복 제거 후).
  - **`is_featured` 가중치 도입**: 기존 상위 300개(전부) + 새 풀 상위 500개(남녀 각)를 "featured"로 표시(`given_name.is_featured`, 마이그레이션 `scripts/db/migrate-add-given-name-featured.js`). `buildCandidates`(`lib/naming/candidates.ts`)가 이 이름들에 소폭 가산점(`config.ts CANDIDATE_SCORE_WEIGHTS.featured = 1`, wonhaeng 만점 6·commonHanja 만점 6보다 훨씬 작게 잡아 지배적이지 않게 함)을 더해, 90점 게이트 통과 풀 안에서 순위를 살짝 올린다 — 상위 비율 무작위 선택(3.6.5)에 조금 더 자주 들어가게 하는 효과만 있을 뿐, 다른 이름을 배제하지 않는다. 회귀 테스트: `candidates.test.ts`("isFeatured 이름은 소폭 가산점으로 실사용 빈도 열세를 뒤집을 수 있다").
- **DB 반영 방식 변경**: `scripts/db/seed-given-names.js`가 기존엔 UPSERT-only(옛 이름이 안 지워짐)였는데, 이번 교체부터 적재 직전 `DELETE FROM given_name`을 추가해 "완전 교체" 스크립트로 바꿨다(DELETE+첫 배치를 하나의 트랜잭션으로 묶어 원자성 확보). `given_name` 테이블의 유일한 출처가 이 파이프라인 하나뿐이라(3.7 원칙 유지), 매번 전량 재적재하는 편이 "누적 UPSERT로 옛 데이터가 안 지워지는" 문제보다 안전하다고 판단했다.
- **한계**: 기존 풀(korean_name.xlsx)과 신규 풀(namechart.kr)의 `frequency` 값 스케일이 서로 다르다(출처·집계 기간이 다름) — 동점자 정렬(3.7)에서 두 출처가 섞인 이름끼리 비교할 때 스케일 차이로 인한 편향이 생길 수 있으나, 영향은 미미하다고 판단해 별도 정규화는 하지 않았다.

### 3.8 만세력 상세 표 — 십신·지장간·공망 (Phase 6 확정, 2026.7.26)

- **배경**: 결과 화면의 사주 정보가 "연주/월주/일주/시주 간지 4칸"뿐이라 디테일이 부족하다는 피드백으로, 전통 만세력 표(십신·지장간·공망)를 추가했다. 이때 십이신살·귀인류(문창귀인·암록·관귀학관·현침살 등 수십 종)까지 전부 넣을지 검토했으나, **1단계(십신·지장간·공망)만 우선 구현하기로 결정**했다. 귀인류는 종류가 수십 개고 각각 별도 대조표가 필요한데, 검증된 출처 없이 표를 채우면 3.5절이 경고한 "출처마다 배속이 갈리는" 문제(石의 金/土 배속 충돌 등)가 그대로 재현될 위험이 크기 때문이다. 12신살(망신살·육해살 등)도 기준지(일지 vs 년지)에 유파차가 있어 함께 보류했다 — **6장 미결정 사항에 등재.**
- **십신(十神) 판정 — 결정적 계산, 유파차 없음**: 일간과 대상 천간의 오행 인덱스 차이(`diff = (대상오행 - 일간오행) mod 5`, `lib/naming/config.ts SANGSAENG_ORDER` 순서 기준)와 음양 동이(同異)로 정해지는 표준 공식이다. `diff` 0/1/2/3/4가 각각 비겁/식상/재성/관성/인성에 대응하고, 음양이 같으면 편(偏)측(비견·식신·편재·편관·편인), 다르면 정(正)측(겁재·상관·정재·정관·정인)이 된다. 지지의 십신은 그 지지의 **정기(正氣, 지장간 중 일수가 가장 큰 지장간)** 천간을 기준으로 판정한다(관례). 일간 자신(일주 천간)도 같은 공식으로 계산하면 항상 비견이 나오므로 특례 없이 그대로 표시한다. `lib/naming/manseryeok.ts`의 `tenGodOf()`. 참고 사주(연주 癸酉/월주 庚申/일주 丙子/시주 丙申, 일간 丙)로 5개 값을 손으로 검산해 일치를 확인했다(`manseryeok.test.ts`).
- **지장간(支藏干) 표시**: 3.3.1의 `BRANCH_HIDDEN_STEMS`(월률분야 일수 비례표)를 그대로 재사용한다 — 새 표를 만들지 않는다. 화면에는 지지 하나당 지장간 전부(2~3개)를 표시하고, 그중 정기(일수 최댓값, 위 십신 판정에도 쓰는 그 항목)를 굵게 구분한다.
- **공망(空亡) 계산 — 결정적 계산, 유파차 없음**: 일주(day pillar)가 60갑자 중 어느 순(旬, 10개씩 6묶음)에 속하는지로 정해지는 표준 대조표(`config.ts VOID_BRANCHES_BY_GROUP`: 갑자순→戌亥, 갑술순→申酉, 갑신순→午未, 갑오순→辰巳, 갑진순→寅卯, 갑인순→子丑)를 쓴다. 60갑자 배열에서 일주 문자열의 인덱스를 찾아 `Math.floor(index/10)`으로 순(旬) 번호를 구하는 방식(`calcVoidBranches()`)이라 예외 케이스가 없다. 계산된 공망 지지 2개는 연·월·일·시 4개 지지 중 해당하는 칸에 표시만 한다(합·형·충 같은 파생 판정은 이번 범위 밖).
- **반합(半合)·형충회합 등은 이번 범위에서 제외**: 삼합 자체는 표준적이지만("申子辰=水局" 등), "부분적 삼합을 반합으로 인정하는지"·"어느 조합까지 유효로 보는지"는 실무에서 갈리고, 이번 요청 범위(1단계)에도 포함되지 않았다. 넣으려면 별도 결정이 필요하다.
- **읽는 순서**: 화면은 전통적으로 오른쪽에서 왼쪽으로 읽는 시주→일주→월주→연주 순서를 좌→우로 그대로 옮겨 시주를 맨 왼쪽에 둔다(`ManseryeokTable.tsx`). 이는 3.6 이전부터 써온 "연주→시주" 순서(요약 문장 등)와는 별개로 전통 만세력 표 관례를 따른 것이다.
- **한글 독음**: 천간·지지 10+12자의 한글 음(`STEM_READING`/`BRANCH_READING`, config.ts)은 표준 한자음 조회일 뿐 판단이 아니라 논쟁의 여지가 없다.
- **사주풀이(감성 해설) — LLM, 2.3 역할 경계 안에서**: 결과 화면에 사주를 은유적·감성적으로 풀어주는 "사주풀이" 섹션을 추가했다. `lib/llm/explain.ts`의 `explainCandidates()` 응답에 `sajuStory: { title, body }`를 추가해 기존 해설·순위와 같은 호출에서 함께 받는다(API 호출 횟수 증가 없음). 프롬프트는 사주·만세력 상세(십신·공망)·오행 분포·신강신약·용신 등 **이미 계산된 사실만** 재료로 주고, "새로운 성격 판단이나 길흉 단정을 만들지 말 것", "직설적이지 않고 따뜻하게, 마지막 문단은 당사자에게 건네는 조언으로 맺을 것"을 명시한다(2026.7.26 브랜드 리네이밍으로 "신생아"·"부모" 한정 표현은 제거 — 위 0.1 참고). 이는 2.1의 "결정(사주·오행·용신 계산)은 코드가 한다"를 건드리지 않는다 — LLM은 이미 확정된 사실을 문학적으로 서술할 뿐, 새 오행·십신·판정을 만들어내지 않는다.

### 3.9 결과 리포트 표시 개선 — 한자 훈음·한글 뜻풀이·오행 표기 (Phase 6 확정, 2026.7.26)

- **한자 훈(訓)·한글 뜻풀이 — LLM 번역, 새 판정 아님**: `hanja.meaning`은 Unihan 영어 뜻풀이만 있고(4.1) 한글 훈(訓)은 DB에 없다. 9,063자 전체에 한글 훈을 미리 채우는 것은 규모가 큰 별도의 데이터 보강(DB 마이그레이션) 작업이라 이번 범위에서는 하지 않았다. 대신 **매 요청마다 화면에 실제로 노출되는 후보 한자(5개 후보 × 2글자 = 10자 안팎)에 한해**, `explainCandidates()`가 반환하는 `NamingReport.candidates[].hanjaGlosses: {char, hun, meaningKo}[]`로 그때그때 만든다. `hun`은 옥편식 훈 한 단어(예: "법"), `meaningKo`는 한글 뜻풀이 한 문장이며, 프롬프트는 DB의 영어 뜻풀이를 근거로 **번역**하도록 명시하고 새 뜻을 창작하지 못하게 한다 — 이는 2.3이 이미 허용한 "확정된 사실을 자연어로 서술"의 연장(번역도 서술의 일종)이지, 새로운 판정(오행·용신·인명용 여부 등)을 만드는 게 아니라서 2.1·2.3과 충돌하지 않는다. 프런트(`CandidateTile.tsx`가 "법 규, 오얏 리" 형태로, `CandidateDetail.tsx`가 자원오행 섹션에 `meaningKo`로 각각 표시)는 `hanjaGlosses`가 비어 있으면(예: report 없음) 조용히 생략하거나 영어 `meaning`으로 폴백한다 — 없는 데이터를 지어내지 않는다.
  - **한계**: 매 요청 생성이라 같은 한자라도 요청마다 `hun`/`meaningKo` 문구가 토씨 하나까지 똑같지는 않을 수 있다(의미는 같음). 전체 한자 DB에 고정된 한글 훈을 채우려면 별도의 데이터 보강 결정이 필요하다 (6장 미결정 사항 추가).
- **종합 해설(`explanation`) 확장**: 기존엔 "3~5문장, 근거 나열" 수준이었는데, 이제 프롬프트가 실제 파이프라인 순서(① 오행 분포·신강신약→용신 ② 발음오행 상생 ③ 인명용/불용문자 필터 통과 ④ 수리사격 81수리 길흉 ⑤ 자원오행·실사용 빈도)를 하나의 이야기처럼 감성적으로 풀어 쓰도록 5~7문장으로 늘렸다. 언급 가능한 근거는 여전히 코드가 계산·검증한 것으로 한정된다(2.3).
- **전문 용어 + 쉬운 풀이 병행 (2026.7.26 보완)**: 신강/신약·용신·발음오행 상생·자원오행·수리사격(원형이정) 같은 용어는 전문성을 위해 그대로 쓰되, 처음 등장할 때 짧은 순화 표현이나 비유를 곁들이도록 `summary`·`sajuStory`·`explanation` 공통 지침을 프롬프트에 추가했다(예: "용신(부족한 기운을 채워 균형을 잡아주는 오행)"). 같은 글 안에서 재등장하는 용어까지 매번 풀어쓰지는 않게 해 문장이 늘어지지 않도록 했다.
- **오행 한자 표기 규칙 — "한자(음)" 병기**: 결과 리포트 어디서든 오행(木火土金水)을 언급할 때 한자만 단독으로 쓰지 않고 "水(수)"처럼 한글 음을 괄호로 병기한다. 코드가 직접 생성하는 텍스트(`yongsin.ts`의 `reason`)는 `config.ts`의 `ELEMENT_READING` 맵으로 고정 처리했고, LLM이 생성하는 텍스트(summary·sajuStory·explanation)는 프롬프트에 이 표기 규칙을 명시했다. 배지형 UI(`ElementBadge` 등, "목(木)" 형태로 이미 한글+한자를 함께 보여줌)는 이번 변경과 무관해 그대로 둔다 — 이번 규칙은 "한자만 단독 노출"되던 자리(코드가 만드는 문장, LLM 산문)에 대한 보완이다.

### 3.10 이름 점수 확인 서비스 — 채점 규칙 (결정적 계산, LLM 미사용, Phase 7 확정)

- **목적**: 3.6~3.7의 후보 **생성** 엔진(`candidates.ts`)은 하드 필터(발음오행 순방향 상생 전부 + 4격 전부 길수)로 "조건을 만족하는 새 이름"만 만든다. 반대로 이미 지어진 임의의 이름은 실제로는 이 하드 필터를 통과하지 못하는 경우가 흔하다(예: 발음오행이 비화이거나 4격 중 하나가 반길인 이름도 실사용에는 많다). 그런 이름을 "0점 불합격"으로 처리하면 서비스로서 무의미하므로, 채점(`lib/naming/score.ts`)은 하드 필터 대신 **등급을 매기는 가중합 점수제**를 쓴다.
- **가중치 (합 100점)**: 발음오행 상생 30점 · 수리사격(4격) 40점 · 자원오행 용신 일치 20점 · 음양 배열 10점. 인명용 한자 여부·불용문자 여부는 점수에 포함하지 않고 별도 경고(`warnings`)로만 반환한다 — "좋고 나쁨"이 아니라 "실사용/신고 가능 여부"에 대한 사실 안내이기 때문이다(3.4와 같은 이유).
- **발음오행 4단계 등급**: 인접한 두 오행의 관계를 상생(순방향, 만점)·비화(같은 오행, 절반)·역상생(뒤 글자가 앞 글자를 생함, 절반)·상극(파괴, 0점)으로 분류한다(`classifyPhoneticLink`). 상극은 `destructiveOf()`(`config.ts`, `SANGSAENG_ORDER`에서 +2칸 위치로 기계적으로 도출, 새 학파 판단 아님)로 양방향 모두 판정한다. 5×5 조합이 정확히 상생5·비화5·역상생5·상극10으로 분류됨을 테스트로 고정했다(`score.test.ts`).
- **수리사격 등급**: 4격 각각 81수리 `fortune`이 길=10점·반길=5점(원래 하드 필터인 `isAuspiciousNumber`는 반길을 흉과 함께 false 처리하지만, 채점은 3단 등급이 필요해 별도 분기)·흉=0점.
- **자원오행 NULL 재환산**: 이름 두 글자 중 `hanja.element`가 있는 글자만 기준으로 채점하고(3.5 "판단 보류" 원칙과 동일), 둘 다 NULL이면 이 항목 자체를 총점 분모에서 제외해 나머지 항목 합계 대비 100점으로 재환산한다. 새 판정을 만들어내는 게 아니라 "이 기준으로는 평가하지 않는다"를 총점 계산에도 일관되게 반영하는 것이다.
- **등급(S/A/B/C/D)**: `totalScore`(90/80/65/50 임계값)를 구간화하는 **표시 전용** 값이다 — LLM도, 새로운 규칙 판단도 아니라 이미 계산된 숫자를 UI가 버킷팅한 것뿐이다.
- **한자 찾기 결과의 한글 훈(訓) 표시 — 공개 라이선스 데이터 재사용 (2026.7.26 확정, 같은 날 2차 갱신)**: 처음엔 `hanja.meaning`(Unihan 영어 뜻풀이)을 그대로 보여줬다가(영어 노출 문제), LLM 호출 없이는 한글 훈이 없어 표시 자체를 생략했었다(LLM은 3.9의 `hanjaGlosses`처럼 유료 서비스에서만 쓸 수 있고, 무료 서비스는 요청당 비용이 0이어야 하므로). 그런데 훈 없이 원획·오행 배지만 나열되니 이름 후보를 고르기 어렵다는 피드백이 있어, **LLM 대신 이미 공개 라이선스로 배포된 훈음 데이터**를 찾아 대체했다: 한국어 위키낱말사전의 "부록:한문 교육용 기초 한자 1800"(CC BY-SA 4.0)은 정부가 공인한 바로 그 1,800자 목록(= `hanja.is_common`의 근거인 Unihan `kKoreanEducationHanja`와 동일 목록)에 대해 "집 가"·"하늘 천" 형식의 훈음표를 이미 공개해 두고 있었다. 이 표를 그대로 재사용한다 — 새 뜻을 창작하는 게 아니라 이미 존재하는, 라이선스가 명확한 자료를 옮기는 것이므로 2.1의 "환각 금지"와 충돌하지 않는다.
  - **1차 재현 파이프라인 (2026.7.26, 1,782자)**: 원본 wikitext 스냅샷 `etc/wiktionary_1800_hanja_raw.txt` → `etc/parse_wiktionary_hun.py`(정규식으로 `[[한자]] <small>([[훈]] 음)</small>` 패턴을 파싱, 훈이 여러 개면(예: 携 = "이끌/가질") 첫 번째만 채택) → `scripts/db/data/hanja_hun.json`(1,787자) → `scripts/db/migrate-add-hun.js`(컬럼 추가) → `scripts/db/seed-hanja-hun.js`(UPDATE, 실제 반영 1,782자 — 5자는 우리 hanja 테이블에 없는 글자).
  - **hanjadic 전면 교체 (2026.7.27, 커버리지 확장)**: 1,800자 밖의 한자(예: 婉·苑처럼 자주 쓰이지만 교육용은 아닌 글자)에 훈이 없다는 피드백에 따라, 출처를 `hanjadic`(오래된 커뮤니티 편찬 한자사전 데이터, happycgi.com 배포판을 파싱해 재배포하는 `NeoMindStd/HanjaDB`, MIT, https://github.com/NeoMindStd/HanjaDB )으로 교체했다. **위키낱말사전(정부 공인 1800자 목록, CC BY-SA 4.0)과 달리 hanjadic은 정부 공인 자료가 아니다** — 저장소 관리자가 "사전 저작권 판례상 데이터 자체엔 저작권이 없다고 간주한다"고 자체 판단해 MIT로 배포하는 것이며, 명시적 오픈 라이선스 문서가 별도로 붙어 있진 않다. 그럼에도 채택한 이유: 훈/음이 실제 원본 한자사전에서 기계적으로 추출되며(새로 창작하지 않음, 2.1 원칙과 충돌 없음), 위키낱말사전보다 수록 범위가 훨씬 넓어(원본 8,140자) 우리 DB 9,063자 중 **7,061자**(약 78%)를 채울 수 있었다.
    - **재현 파이프라인**: 저장소의 `input/resource` 원본을 그대로 받은 `etc/hanjadic_raw.txt`(10,263줄) → `etc/parse_hanjadic_hun.py` — 줄마다 `한자=훈1 음1, 훈2 음2, ..., 영어뜻 (획수)` 형식을 파싱해 콤마 구간 중 순수 한글인 부분만 훈/음 후보로 모으고("○의 略字"처럼 한자가 섞인 이체자 설명 구간은 훈/음이 아니므로 자동으로 걸러짐), 우리 DB(`final_hanja.json`)의 **공식 인명용 readings와 음이 일치하는 후보만** 채택한다(일치하는 후보가 없으면 추측하지 않고 건너뜀 — 2.1 환각 금지) → `scripts/db/data/hanja_hun.json`(7,046자, 기존 1,782자 중 겹치는 값도 hanjadic 기준으로 덮어씀) → `scripts/db/seed-hanja-hun.js`(스크립트 변경 없이 재사용, UPDATE) → 실제 반영 7,061자(7,046 + hanjadic에 없어 기존 위키낱말사전 값이 그대로 남은 15자).
  - **커버리지 한계**: 9,063자 중 여전히 약 2,002자(≈22%)는 `hanja.hun`이 NULL이다 — hanjadic에도 없는 극희귀 한자(예: 邧처럼 지명 전용 한자)이거나, DB의 공식 readings와 hanjadic 표기 음이 일치하지 않는 극소수 사례다. 검색 결과에는 이 경우 훈 없이 음만 표시한다. `hanja.meaning`(영어)은 한자 찾기 화면 어디에도 노출하지 않는다.
  - **크레딧 비용 0**: LLM을 전혀 호출하지 않았다 — 두 출처(위키낱말사전, hanjadic) 모두 이미 번역·정리가 끝난 공개 자료라서 가능했다.
- **한자 찾기(`getHanjaByReading`, `/api/hanja-search`)**: 한글 음 검색 결과를 `is_name_allowed`/`is_forbidden`로 걸러내지 않고 전부 보여준다 — 실제로 쓰이는 이름은 9,063자 등록 목록 밖 한자나 불용문자를 쓰고 있을 수도 있으므로, 검색 자체를 막지 않고 결과에 경고 배지만 붙인다(정렬만 `is_name_allowed DESC, is_common DESC, stroke_original ASC`로 유효한 것을 위로 올림). 단, **`hun`(한글 훈)이 NULL인 한자는 제외한다**(2026.7.27) — 이건 인명용/불용 여부와 무관한 별도 조건으로, 훈이 없으면 사용자가 뜻을 몰라 후보로 고르기 어렵다는 실사용 피드백에 따른 것이다. 3.10 위 hanjadic 갱신으로 9,063자 중 7,061자에 훈이 있어(2,002자는 검색 결과에서 안 보임), 대부분의 실사용 검색에는 영향이 크지 않다.
- **성별 불필요**: 채점은 `given_name`(3.7) 큐레이션 풀을 쓰지 않는다(이미 정해진 이름을 채점하는 것이라 후보 풀이 필요 없음) — `/api/score` 요청에 `gender` 필드가 없다.
- **범위 제약**: `GIVEN_NAME_LENGTH=2`와 동일한 이유로 이름 두 글자만 지원한다(1글자·3글자 이상은 6장 미결정 사항).

---

## 4. 데이터 모델 (초안 — Turso)

### 4.1 hanja (한자 사전)

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | INTEGER PK | |
| char | TEXT | 한자 (유니코드) |
| readings | TEXT | 인명용 대표음(들), JSON 배열 |
| stroke_original | INTEGER | 원획 수 (수리 계산용) |
| stroke_actual | INTEGER | 필획 수 (참고용) |
| radical | TEXT | 부수 |
| element | TEXT | 자원오행 (木/火/土/金/水) |
| meaning | TEXT | 뜻풀이 |
| is_name_allowed | INTEGER | 인명용 한자 여부 (0/1) |
| is_forbidden | INTEGER | 불용문자 여부 (0/1) |
| forbidden_reason | TEXT | 불용 사유 (nullable) |
| verification_status | TEXT | `confirmed`/`unverified`. 현재는 `confirmed`만 적재 (아래 "Phase 3 데이터 출처" 참고). |
| is_common | INTEGER | 교육용 기초한자(Unihan `kKoreanEducationHanja`, 1,800자) 포함 여부 (0/1). 후보 생성 가산점 전용 (3.6). Phase 6에서 추가, 9,063자 중 1,796자가 1. 재현: `etc/add_common_hanja_flag.py` → `scripts/db/migrate-add-is-common.js`(컬럼 추가) → `scripts/db/seed-hanja.js`(값 반영). |
| hun | TEXT | 한글 훈(訓) 한 단어(예: "하늘"), nullable. 무료 "한자 찾기"(3.10) 표시 전용. LLM 번역 아님, 크레딧 비용 없음. Phase 7에서 위키낱말사전 "부록:한문 교육용 기초 한자 1800"(CC BY-SA 4.0)으로 1,782자 우선 적재 후, 2026.7.27 hanjadic(`NeoMindStd/HanjaDB`, MIT — 정부 공인 자료는 아님, 3.10 참고)으로 전면 교체해 9,063자 중 **7,061자**가 값 있음. 재현: `etc/parse_hanjadic_hun.py`(원본 `etc/hanjadic_raw.txt`) → `scripts/db/seed-hanja-hun.js`(값 반영, 컬럼 자체는 `scripts/db/migrate-add-hun.js`로 이미 추가됨). |

**Phase 3 데이터 출처 및 한계 (2차 갱신)**: 처음에는 대법원 공식 "인명용 한자표" PDF가 이미지 기반이라 OCR(Tesseract)로 판독했는데, 이 서체에서 인식률이 낮아 Unicode Unihan DB 독음 교차검증을 통과한 **1,789자만** 확보했었다. 이후 사용자가 대법원 전자가족관계등록시스템(efamily.scourt.go.kr) 조회 결과를 전수 수집한 텍스트 기반 표(`인명용한자_전체표.docx`, 9,460자, 한자·유니코드 코드포인트·등록된 모든 음 포함)를 제공해 **이 표를 1차 출처로 교체**했다. 교체 전 기존 확정 1,789자와 교차검증한 결과 1,781자(99.6%)가 정확히 일치하고 독음 불일치가 0건이어서 신뢰도를 확인한 뒤 반영했다.

- **최종 적재: 9,063자.** 9,460자 중 원획/필획/부수 계산에 필요한 Unihan 데이터가 없는 405자(대부분 매우 희귀한 확장 한자로, 문서 자체도 "일부 희귀 한자는 글꼴에 따라 표시 안 될 수 있다"고 밝힘)는 **추측하지 않고 이번 적재에서 제외**했다. 대신 새 표에는 없지만 기존에 Unihan `kKoreanEducationHanja`(교육용 기초한자 공식 플래그) 근거로 이미 확정했던 8자(努 奴 姬 嫩 怒 港 珊 𮕩)는 근거가 있으므로 유지했다.
- 원획/필획/부수/자원오행은 여전히 Unihan DB에서 결정적으로 계산한다 (문자 목록 출처가 바뀌어도 이 계산 로직 자체는 변하지 않음, 3.2·3.5 참고).
- 새 표 자체도 "공식 수치(9,389)와 소폭 차이가 있을 수 있다"는 문서 자체의 경고가 있어, **법적 효력이 필요한 실사용 단계에서는 대법원 시스템 원본 재확인이 필요**하다 (7장 법적 주의사항과 일치).
- 재현 스크립트: `ETC/parse_full_hanja_table.py`(원본 파싱) → `ETC/build_final_hanja_v2.py`(Unihan 결합·불용문자 매칭) → `scripts/db/seed-hanja.js`(적재).

### 4.2 surname (성씨)

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | INTEGER PK | |
| hangul | TEXT | 한글 성 (예: 김) |
| hanja | TEXT | 한자 성 (예: 金) |
| stroke_original | INTEGER | 성씨 원획 |
| initial_element | TEXT | 초성 발음오행 |

### 4.3 numerology_81 (81수리 길흉표)

| 컬럼 | 타입 | 설명 |
|---|---|---|
| number | INTEGER PK | 1~81 |
| fortune | TEXT | 길/흉/반길 등 |
| title | TEXT | 수리 명칭 |
| description | TEXT | 해설 |

> 성씨별 길한 수리 조합, 성씨별 상생 가능한 이름 초성 조합은 성씨 획수·초성이 고정값이므로 사전 계산해 캐시 테이블로 두는 것을 권장.

> **출처**: 81수리(熊崎式 수리성명학 계열) 1~81 전체 길흉/해설. `fortune`은 `길`/`흉`/`반길` 중 하나(반길 = 선흉후길 등 혼합 판정, `isAuspiciousNumber()`는 반길을 흉과 함께 false로 보수적 처리 — `lib/naming/numerology.ts` 참고).

### 4.4 surname (2026.7.28 갱신 — 100개로 확장)

- **출처**: 사용자 제공 `etc/korean_sung.hwp`(한글과컴퓨터 HWP 포맷, 인구 기준 상위 100개 성씨 순위표, 한글+한자). `hwp5txt`(pyhwp)로 텍스트 추출 후 수작업으로 검증해 `scripts/db/data/surname.json`에 반영했다. 기존엔 2015 인구총조사 상위권 30개만 있었다.
- **적재 현황**: 100개 항목이 실제로는 **102행**이다 — 두음법칙으로 하나의 한자를 두 가지 한글로 읽는 항목(19번 유/류=柳, 44번 나/라=羅)은 각각 별도 행으로 분리해 두 표기 모두 성씨 조회(`getSurnameByHangul`)에서 찾을 수 있게 했다. `stroke_original`은 새로 계산하지 않고 이미 확정된 `hanja` 테이블(9,063자, 4.1 참고)의 원획 값을 그대로 조회해 채웠다 — 같은 계산 로직을 두 번 구현하지 않기 위함이며, 필요한 한자 전부가 이미 hanja 테이블에 존재함을 확인했다.
- **복성(複姓) 처리**: 91번 남궁(南宮)은 2음절 성씨라 `hangul="남궁"`(2글자) 한 행으로 저장했다. `stroke_original`은 南(9)+宮(10)=19로, **두 글자 원획의 단순 합**을 성씨 원획 하나로 취급한다 — 전통 성명학에서 복성은 별도의 오격(五格) 체계를 쓰는 유파도 있지만, `calcSagyeok`(4격 계산, `lib/naming/numerology.ts`)이 "성 1자" 전제로 설계되어 있어 이번 확장에서는 기존 4격 계산 로직을 그대로 재사용하는 단순화를 택했다. 더 정교한 복성 전용 격 체계가 필요해지면 별도 결정이 필요하다(6장 참고). `initial_element`는 기존과 동일하게 첫 음절("남")의 초성 기준으로 계산되므로 복성이라고 특별 처리가 필요하지 않았다.
- `stroke_original`은 hanja 테이블 원획 기준(궁극적으로 Unihan DB 기준, 4.1 참고), `initial_element`는 `config.ts`의 `INITIAL_CONSONANT_ELEMENT`로 계산 — 계산 로직 자체는 이번 확장으로 바뀌지 않았다.
- 100개 밖의 희귀 성씨·이체자는 여전히 없다 — 6장 미결정 사항 갱신 참고.

### 4.5 given_name (이름 후보 풀, Phase 6 확정 — 3.7·3.7.1 참고)

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | INTEGER PK | |
| hangul | TEXT | 한글 이름 2글자 (예: "서준") |
| gender | TEXT | `M`/`F` |
| frequency | INTEGER | 원본 표의 실사용 빈도(순위 아님, 클수록 많이 쓰임) — 2026.8.2부터 출처가 섞여 있어 스케일이 균일하지 않음(3.7.1 한계 참고) |
| is_featured | INTEGER | 2026.8.2 추가. 추천 시 소폭 가중치를 줄 이름인지(0/1) — 기존 상위 300개(남녀 각) + namechart.kr 신규 풀 상위 500개(남녀 각). `lib/naming/candidates.ts CANDIDATE_SCORE_WEIGHTS.featured` |

- **출처(2026.8.2 갱신, 3.7.1 참고)**: `etc/korean_name.xlsx` 기반 기존 풀 중 `frequency` 상위 300개(남녀 각) + `namechart.kr` 2020~2026 수집 기반 신규 풀 전체. 재현: `etc/scrape_namechart.py`(수집, 이후 필터 단계는 3.7.1에 서술된 기준으로 인라인 실행돼 스크립트 미보존) → `etc/final_name_{male,female}.csv` → `etc/build_final_given_names.py`(기존 상위 300 병합 + featured 표시) → `scripts/db/data/given_names.json` → `scripts/db/migrate-add-given-name-featured.js`(컬럼 추가, 1회) → `scripts/db/seed-given-names.js`(DELETE 후 전량 재적재).
- **적재 현황**: 여자 2,056개, 남자 3,092개(고유 이름 기준, 2글자만, featured 여 521개·남 520개). 이전(`etc/korean_name.xlsx` 단독, 여 1,442/남 1,448)보다 여자 약 1.4배, 남자 약 2.1배로 확장됐다. 1글자/3~4글자 이름은 여전히 후보로 나올 수 없다(6장 참고).
- `buildCandidates`(3.7)는 이 테이블에 있는 이름만 후보로 삼는다 — 한자 자유 조합은 하지 않는다. `is_featured` 이름은 배제되지 않고 다른 이름과 동등하게 게이트(90점)를 통과해야 하며, 그 위에 소폭 가산점만 더해진다(3.7.1).

### 4.6 이름 점수 확인 서비스 — 새 테이블 없음 (Phase 7)

채점(3.10)은 `hanja`/`surname`/`numerology_81` 기존 테이블만 조회하며, 새 테이블이나 컬럼을 추가하지 않는다. `lib/db.ts`에 `getHanjaByReading(reading)`(한글 음 → 한자 후보 목록, `json_each(readings)`로 조회)만 추가했다 — `readings` 컬럼에 인덱스는 없지만 9,063행 규모라 풀스캔으로도 충분하다.

### 4.7 user / session / score_result / naming_result (로그인 베타, Phase 9 확정, 2026.7.28)

| 테이블 | 컬럼 | 설명 |
|---|---|---|
| `user` | id, email(UNIQUE), password_hash, display_name, phone, real_name, created_at | `password_hash`는 `"scrypt:v1:<saltHex>:<hashHex>"` 형식(0.2 참고), SNS 전용 계정은 `"oauth:v1:<provider>"` sentinel(0.3 참고). `display_name`은 소셜 닉네임(nullable, Phase 10). `phone`(소셜 로그인 시 제공자가 내려준 휴대전화번호)·`real_name`(네이버 "회원이름"/실명, 카카오는 항상 NULL)은 0.3 "이메일/닉네임 파싱 버그 수정" 항목에서 추가(nullable). |
| `session` | id, user_id, created_at, expires_at | `id`엔 원본 토큰이 아니라 **SHA-256 해시**만 저장(0.2 참고). `expires_at`은 INSERT 시 `datetime('now','+30 days')`로 SQL이 계산. 이메일/비밀번호 로그인 전용 — SNS 로그인은 next-auth 자체 JWT 세션을 쓴다(0.3). |
| `oauth_account` | id, user_id, provider, provider_account_id, created_at | SNS 로그인(0.3, Phase 10) — 어떤 소셜 계정이 어떤 회원과 연결되는지. `UNIQUE(provider, provider_account_id)`. |
| `score_result` | id, user_id, request_payload(JSON), result(JSON), created_at | 무료 서비스 결과. `result`는 `ScoreApiResult`를 그대로 직렬화 — 재계산 없이 화면 재현 가능. 영구 보관, 삭제는 `DELETE /api/history/score/[id]`. |
| `naming_result` | id, user_id, request_payload(JSON), result(JSON), created_at, expires_at | 프리미엄 결과. `result`엔 비용이 든 LLM `report`까지 포함 — 재열람 시 LLM을 다시 호출하지 않기 위해 verbatim 저장. `expires_at`도 INSERT 시 `datetime('now','+30 days')`로 계산, 조회 시 이 조건으로 필터링(별도 삭제 기능 없음).

기존 4테이블(hanja/surname/numerology_81/given_name)과 동일하게 **FK 제약을 쓰지 않는다** — 소유권은 매 조회 SQL의 `WHERE ... AND user_id = ?`(+ 필요시 `expires_at` 조건)로 강제한다. `lib/db.ts`(읽기 전용 참조 데이터)와 성격이 달라 쓰기 전용 접근 함수는 신규 `lib/db-auth.ts`로 분리했다(8.2 참고).

### 4.8 price_tier / payment_order (결제, Phase 14 확정, 2026.8.4)

| 테이블 | 컬럼 | 설명 |
|---|---|---|
| `price_tier` | candidate_count(PK, 3/5/10), price, updated_at | 관리자가 `/admin/pricing`에서 직접 갱신(0.4 참고). 코드 상수가 아니라 DB에 두어 배포 없이 가격을 바꿀 수 있게 했다. |
| `payment_order` | id, user_id, candidate_count, amount, status, provider, provider_order_id, naming_result_id, created_at, updated_at | 결제 주문 1건 = "candidate_count개 생성 1회 이용권". `amount`는 주문 시점 `price_tier` 가격 스냅샷(이후 가격이 바뀌어도 이미 만든 주문엔 영향 없음). `status`는 `pending → paid`(페이앱 웹훅으로만 전이, 유일한 신뢰 소스) `→ consumed`(`app/api/name`이 원자적 UPDATE로 claim) 순으로 진행하거나 `paid` 이전에 `canceled`/`failed`로 끝난다. `provider_order_id`는 페이앱 `mul_no`. |

기존 테이블들과 동일하게 FK 제약을 쓰지 않고 `WHERE user_id = ?`로 소유권을 강제한다. 웹훅(`app/api/payment/webhook`)만 예외적으로 `user_id` 없이 조회한다 — 웹훅은 유저 세션이 아니라 페이앱의 `userid`/`linkkey`/`linkval`로 인증되기 때문이다(0.4 참고).

### 4.9 inquiry (회원 문의하기, Phase 15 확정, 2026.8.4)

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | INTEGER PK | |
| user_id | INTEGER NOT NULL | 소유권 강제는 조회 시 `WHERE user_id = ?`(FK 없음, 기존 관례와 동일). |
| content | TEXT NOT NULL | 회원이 작성한 문의 본문(텍스트만). |
| answer | TEXT | 관리자 답변(nullable). `NULL`이면 답변대기, 값이 있으면 답변완료 — 별도 status 컬럼 없음(0.5). |
| answered_at | TEXT | 답변 등록 시각(nullable, `UPDATE ... SET answered_at = CURRENT_TIMESTAMP`로 채워짐). |
| created_at | TEXT NOT NULL | 문의 등록 시각. |

FK 제약 없음, `idx_inquiry_user_id` 인덱스만 둔다. 영구 보관(자동 만료 없음), 삭제는 관리자 전용 하드 삭제(`deleteInquiryAdmin`, `user_id` 조건 없이 id만으로 삭제).

---

## 5. 개발 로드맵

- [x] **Phase 1 — 뼈대 + 배포**: Next.js + TS + vercel 배포 완료, 실제 URL에서 Hello World 확인 완료. (Turso 실제 DB 생성·연결은 Phase 3에서 완료 — `.env.local`에 자격정보 있음, git에는 커밋 안 됨.)
- [x] **Phase 2 — 사주 엔진**: 생년월일시 → 팔자 → 오행분포 → 신강약 → 용신. 순수 함수 + 테스트(vitest, 9개 통과) 완료. UI/DB 연동 없음 (`app/api/name/route.ts`는 아직 미조립).
- [x] **Phase 3 — 참조 데이터 (대부분 완료)**: Turso 실제 DB 생성·연결 완료, `hanja`(**9,063자**, 대법원 시스템 수집 텍스트 표 기준 — 4.1 "Phase 3 데이터 출처" 참고)/`surname`(30개)/`numerology_81`(81개 전체) 테이블 적재 완료. `lib/db.ts`, `lib/naming/numerology.ts` 구현 및 테스트 완료. 남은 갭: 원획 계산이 안 되는 희귀 한자 405자 제외됨, 자원오행 배속표는 214부수 중 19개만 채워진 부분표, 성씨는 30개뿐. 6장 미결정 사항 참고.
- [x] **Phase 4 — 작명 엔진**: 용신 + 성씨 제약으로 후보 조합 생성. `lib/naming/candidates.ts`(발음오행 상생 + 4격 전부 길수 하드 필터, 자원오행 일치·음양 균형 가산점) 구현 및 테스트(vitest) 완료, `app/api/name/route.ts`에서 saju→db→yongsin→candidates 파이프라인 조립 완료.
- [x] **Phase 5 — LLM 설명 계층**: Claude API로 후보 큐레이션 + 해설 생성. `lib/llm/explain.ts` 신설 — `lib/naming/` 밖에 두어 8.1 원칙(naming 폴더는 LLM을 import하지 않음)을 물리적으로 지켰다. 이미 확정된 사실(사주·오행 분포·용신·후보 획수/발음오행)만 프롬프트에 담아 전달하고, `output_config.format`(json_schema)으로 출력을 강제해 LLM이 새 수치·판정을 만들지 못하게 했다(2.3). `app/api/name/route.ts`에서 `buildCandidates` 이후에만 호출하도록 조립(8.2). 모델은 `claude-opus-4-8`. 타입 체크·기존 vitest 29개 통과 확인; 실제 API 호출 스모크 테스트는 비용이 발생해 사용자 승인 전 보류 — 실사용 전 확인 필요.
- [x] **Phase 6 — 프론트/UX + 마무리**: `design.md` 기준 입력 폼(단계별 위저드)·결과 리포트(벤토 그리드 + 점진적 공개)·다크모드·법적 고지 1차 구현 완료. 베타 실사용 중 발견된 작명 엔진 버그(후보 다양성 부재, 3.6 참고)를 수정하고 `is_common` 컬럼을 추가해 한자 친숙도를 개선했다. 이후 사용자가 제공한 실사용 이름 표로 이름 후보 풀 자체를 자유 조합에서 큐레이션 목록 기반으로 전환했다(3.7, `given_name` 테이블) — 성별(`gender`) 입력이 이제 실제로 쓰여 입력 폼에 추가됨. 결과 화면의 디테일 부족 피드백에 대응해 만세력 상세 표(십신·지장간·공망, `lib/naming/manseryeok.ts`, 3.8)와 LLM 기반 사주풀이 감성 해설(`sajuStory`, 3.8)을 추가했다. 이어서 후보 한자의 훈음·한글 뜻풀이 표시(`hanjaGlosses`, 3.9), 종합 해설의 파이프라인 근거 서술 확장, 오행 한자 "한자(음)" 병기 규칙(3.9)을 추가했다.
- [ ] **Phase 7 — 무료 "이름 점수 확인" 서비스 + 라우팅 분리 (진행 중, 2026.7.26)**: 서비스를 두 티어(0.1)로 분리했다. 라우팅을 처음 도입해 `/`(랜딩, 두 서비스 카드)·`/naming`(기존 유료 위저드+결과, 파일만 이동)·`/score`(신규 무료)로 나눴다. 채점 로직(`lib/naming/score.ts`, 3.10)을 순수 함수로 신설하고 `app/api/score/route.ts`에서 LLM 없이 조립했다(`explainCandidates` 미호출). "한자 찾기"(`getHanjaByReading`, `/api/hanja-search`, `HanjaSearchPicker.tsx`)를 신설했다. `InputForm`과 `NameScoreForm`이 처음부터 동시에 필요로 하는 생년월일시 입력을 `BirthDateTimeFields.tsx`로 공유 추출했고(8.3), `LoadingStages`를 stages prop 받는 범용 컴포넌트로 일반화했다. `candidates.ts`의 `isYinYangBalanced`를 `score.ts`로 옮겨 생성·채점 양쪽이 공유한다. vitest(`score.test.ts`, 5×5 상생/비화/역상생/상극 분류 전수 검증 포함)·tsc·eslint 통과 확인. 남은 것: 브라우저 실기 확인(design.md 검증 절차), 실제 결제 게이트 연동(현재 "유료"는 개념뿐, 6장 참고).
- [x] **Phase 8 — 프리미엄 작명 후보 개수 선택 · 순위 폐지 · 유사 이름 소프트 필터 (2026.7.27)**: 3.6.1 참고. `CANDIDATE_COUNT_OPTIONS=[3,5,10]`로 후보 개수를 선택형으로 바꿨고(`InputForm`·`/api/name`), LLM `rank` 필드를 제거해 순위를 없앤 대신 결정적으로 계산되는 강점 태그(`Candidate.highlights`)를 신설해 벤토 타일을 동일 크기로 통일했다(`HighlightBadges.tsx`, `CandidateDetail.tsx`). 표시 순서는 `app/lib/shuffle.ts`로 응답 직전 무작위화한다. `lib/naming/similarity.ts`(자모 단위 유사도)로 "규리/규린/규나"류 이름이 한 결과에 몰리는 것을 완화 단계별 소프트 필터(`CANDIDATE_DIVERSITY.stages.avoidSimilar`)로 최대한 거른다. vitest(`similarity.test.ts`, `candidates.test.ts` 갱신)·tsc 통과 확인.
- [x] **Phase 9 — 로그인(이메일/비밀번호) 베타 + 마이페이지 (2026.7.28)**: 0.2·4.7 참고. `lib/auth.ts`(비밀번호 scrypt 해시, DB-backed opaque 세션)·`lib/db-auth.ts`(user/session/score_result/naming_result CRUD) 신설, `app/api/auth/{signup,login,logout}` 라우트 추가. `app/api/name/route.ts`는 최상단 401 게이트(로그인 필수) + 성공 시 `naming_result` best-effort 저장을 추가했고, `app/api/score/route.ts`는 게이트 없이 로그인 상태일 때만 자동 저장하도록 했다. `app/naming/page.tsx`를 Server Component로 바꾸고 기존 위저드 로직은 `NamingWizardClient.tsx`로 옮겨, 비로그인 최종 제출 시 `AuthRequiredModal`을 띄우고 `sessionStorage`로 입력값을 보존했다가 로그인 후 `/naming?resume=1`에서 복원한다(폼 재입력 방지). `Nav.tsx`는 이미 Server Component였음을 확인하고 `async` 전환 후 `getCurrentUser()`를 직접 호출해 로그인 상태별 링크(로그인/회원가입 ↔ 마이페이지/로그아웃)를 표시한다 — 이로 인해 Nav를 포함한 모든 라우트가 정적 프리렌더링에서 동적 렌더링으로 바뀌는 트레이드오프를 받아들였다. `app/mypage/*` 페이지에서 저장된 결과를 `ScoreDashboard`/`ResultsDashboard`(신규 `restartLabel` prop으로 문구만 다르게)로 그대로 재현한다. `GET /api/history/*` 같은 조회용 API는 만들지 않고 마이페이지 Server Component가 `lib/db-auth.ts`를 직접 호출하며, 클라이언트 인터랙션이 필요한 삭제(`DELETE /api/history/score/[id]`)만 API로 뒀다(8.3). vitest(`lib/auth.test.ts`, 해시/세션 토큰 유일성)·tsc·eslint 통과 확인.
- [x] **Phase 10 — SNS 로그인(카카오/네이버, 최초엔 구글 포함) (2026.7.31)**: 0.3·4.7 참고. 사업자 등록 완료로 0.2에서 미뤄뒀던 소셜 로그인을 붙였다. `next-auth`(Auth.js v5 beta) 설치, `lib/oauth.ts`에 구글/카카오/네이버 프로바이더와 jwt/session 콜백을 설정해 `lib/db-auth.ts`의 신규 `findOrCreateOAuthUser`(provider_account_id 기준 매칭, 이메일 일치 시 자동 연동)로 우리 `user` 테이블과 연결한다. `oauth_account` 테이블 신설, `user.display_name` 컬럼 추가(둘 다 스키마 마이그레이션, 기존 데이터 영향 없음). `lib/auth.ts`의 `getCurrentUser()`가 커스텀 쿠키 세션 다음으로 next-auth JWT 세션을 확인하도록 확장 — 이 한 지점만 넓혀 `/naming`·`/score`·마이페이지·관리자 판별 등 나머지 코드는 변경 없이 그대로 동작한다. `app/api/auth/[...nextauth]/route.ts`(handlers 노출), `app/components/SocialLoginButtons.tsx`(Server Action 기반 로그인 버튼, 로그인/회원가입 페이지에 배치), `next-auth.d.ts`(타입 보강) 추가. 로그아웃은 커스텀 세션과 next-auth 세션을 모두 정리한다. tsc·eslint·vitest(81개, `lib/auth.test.ts`에 `./oauth` 스텁 추가 — next-auth가 next/server를 불러와 순수 Node 테스트 환경과 맞지 않아 db-auth와 동일하게 모킹) 통과 확인. **같은 날 구글 연동 제거**(0.3 "구글 제거" 참고) — 이후 지원 제공자는 카카오/네이버 둘뿐이다. 각 제공자 앱 등록·Client ID/Secret 발급은 사용자가 직접 진행.
- [x] **Phase 14 — 결제(페이앱) 연동 (2026.8.4)**: 0.4·4.8 참고. 카드·카카오페이·네이버페이 3종, 추천 개수(3/5/10)별 5,900/8,900/14,900원. `lib/payment/`(PG 추상화, `PaymentProvider` 인터페이스 + `payapp.ts` 구현)·`lib/db-payment.ts`(`price_tier`/`payment_order` CRUD, 원자적 `claimPaymentOrder`) 신설. `app/api/payment/{checkout,webhook,orders/[id],price-tiers}` 라우트 추가, `app/api/name/route.ts`에 결제 소비 게이트(성씨 검증 등 무료 재시도 가능한 단계 통과 후, LLM 호출 직전에 claim) 배선. `app/admin/pricing`(Server Action)으로 관리자가 가격 직접 수정. 프론트는 `AuthRequiredModal`과 동일한 sessionStorage 재개 패턴으로 `PaymentRequiredModal`/`PaymentCheckoutButton`을 `InputForm`/`NamingWizardClient`에 배선(모바일 풀리다이렉트 복귀 포함). `app/naming/page.tsx`의 관리자 전용 게이트는 그대로 유지 — 이번 결제 흐름은 관리자 계정으로만 도달 가능하며, 실결제 검증은 사용자 본인이 진행.
- [x] **Phase 15 — 회원 문의하기 (2026.8.4)**: 0.5·4.9 참고. 로그인 회원이 `/mypage/inquiries`에서 텍스트 문의를 등록하면 관리자가 `/admin/inquiries`에서 답변하는 최소 문의 시스템. `inquiry` 테이블 신설(`user_id`·`content`·`answer`·`answered_at`·`created_at`, FK 없음). `lib/db-auth.ts`에 CRUD 추가(`createInquiry`/`listInquiriesByUser`/`listAllInquiriesForAdmin`/`answerInquiry`/`deleteInquiryAdmin`), `deleteUserAccount`에도 정리 로직 추가. `app/api/inquiry`(등록)·`app/api/admin/inquiries/[id]`(답변·삭제) 라우트, 회원용 `InquiryPageClient.tsx`·관리자용 `AdminInquiryPanel.tsx`(둘 다 낙관적 목록 갱신) 추가. `app/admin/page.tsx` 허브와 `app/mypage/page.tsx`에 진입 링크 배선. tsc·eslint·`next build` 통과 확인.

---

## 6. 미결정 사항 (진행하며 확정)

- 조후용신 우선 적용 기준의 구체적 임계값.
- **후보 선정 점수의 가중치 (3.6 재조정, 3.6.3 재설계 완료, 2026.8.1)**: `wonhaengMatch`/`commonHanja` 만점을 3/3(=6점)로 동률로 맞춘 2026.7.26 결정은 유지하되, wonhaeng 쪽은 이진 매치(0/1/2)에서 주/보조 용신 차등 + 오행분포 부족량 비례 연속 점수로 재설계됐다(3.6.3 — 같은 성씨면 사주가 달라도 상위 후보가 반복되던 문제의 대응). 여전히 실사용 데이터 없이 판단한 값이라 더 많은 피드백이 쌓이면 재조정이 필요할 수 있다. 이 점수는 선택된 개수(3.6.1) 안에서 어떤 후보를 뽑을지에만 관여하며, 화면 표시 순서는 순위가 아니라 무작위화된 순서다(3.6.1 — Phase 8에서 LLM `rank` 재배열을 폐지).
- 후보 순위 알고리즘의 가중치 (수리 vs 발음 vs 자원오행 vs 친숙도(`is_common`) 비중) — 위 항목과 별개로, 수리(81수리)·발음오행은 현재 하드 필터(통과/탈락만 있고 점수화되지 않음)라 소프트 스코어에 아직 반영되지 않는다. 필요해지면 하드 필터를 소프트 스코어로 바꿀지 별도 결정 필요. 이 하드 필터가 "성씨당 고정 통과 풀"을 만드는 근본 원인이라(3.6.3), 근본적으로 다양성을 늘리려면 이 미결정이 함께 다뤄져야 한다.
- 항렬자 지원 여부.
- **이름 후보 풀(3.7·3.7.1 `given_name`) 확장** — 2026.8.2 재구성으로 여 2,056개·남 3,092개(2글자만)까지 넓어졌지만, 여전히 1글자·3~4글자 이름과 두 원본 표 밖의 희귀·개성적인 이름은 후보로 나올 수 없다. 더 넓히려면 또 다른 공식 출처를 추가하는 결정이 필요하다.
- **namechart.kr 필터 파이프라인 재현성 갭 (3.7.1, 2026.8.2 신규)**: 2글자 필터·수집(`scrape_namechart.py`)까지는 스크립트로 남아 있지만, 그 뒤 어감 규칙·서양식/종교 이름 제거·고유명사 제거 단계는 대화 중 인라인 실행이라 스크립트 파일로 저장되지 않았다. 다음에 이 풀을 다시 조정하려면 3.7.1의 기준 서술을 보고 다시 구현해야 한다 — 스크립트로 정리해두는 게 나을지는 재조정 빈도를 보고 판단.
- **만세력 표의 12신살·귀인류 확장 (3.8 관련, 2026.7.26 보류)**: 십신·지장간·공망은 유파차가 없는 결정적 계산이라 구현했지만, 12신살(망신살·육해살·장성살 등, 기준지가 일지/년지 유파차 있음)과 귀인류(문창귀인·암록·관귀학관·현침살 등 수십 종, 각각 별도 대조표 필요)는 검증된 단일 출처 없이 채우면 3.5절과 같은 배속 충돌 위험이 커서 보류했다. 넣으려면 특정 참고서적·유파 하나를 선택해 그 대조표를 전부 따르는 결정이 필요하다.
- **한자 한글 훈(訓) DB 보강 — LLM 해설(`hanjaGlosses`)은 여전히 별개 (3.9 관련, 2026.7.26 보류)**: `hanja.hun` 컬럼(3.10)은 2026.7.27 hanjadic 전면 교체로 9,063자 중 7,061자까지 채워졌지만, 유료 서비스의 `explainCandidates()`가 화면에 노출되는 후보 한자에 대해 매 요청마다 LLM으로 만드는 `hanjaGlosses`(3.9, "번역"이지 `hun` 컬럼과는 별개 산출물)는 이번 변경과 무관하게 그대로다. `hun`이 채워진 한자라도 `hanjaGlosses`는 여전히 요청 시점에 다시 생성된다 — 두 값을 통합할지(예: `hun`이 있으면 LLM 호출 생략)는 아직 결정하지 않았다.
- **hanjadic 미매칭 2,002자 및 출처 신뢰도 재검토 (3.10 관련, 2026.7.27 신규)**: hanjadic에도 없거나 공식 readings와 음이 불일치하는 약 2,002자는 `hun`이 여전히 NULL이다. 또한 hanjadic 자체가 정부 공인 자료가 아니라 커뮤니티 편찬 사전이라(3.10 참고), 법적 효력이 필요한 맥락(7장)에서는 이 훈 표시를 참고용 이상으로 취급하면 안 된다 — 필요해지면 대법원·국립국어원 등 공식 훈음 출처 확인이 별도로 필요하다.
- **인명용 한자 DB 커버리지 확장 (2026.7.25 완료)**: `ETC/인명용한자_전체표.docx`(사용자 제공, 대법원 전자가족관계등록시스템 전수 수집 텍스트 표, 9,460자)로 1차 출처를 교체해 **9,063자**를 적재했다 (교체 전 기존 OCR+Unihan 확정 1,789자와 교차검증 — 1,781자/99.6% 일치, 독음 불일치 0건 확인 후 반영). 남은 갭:
  - 405자는 Unihan에 원획/필획 계산 근거가 없어 **추측하지 않고 제외**했다 (대부분 극희귀 확장한자 — `ETC/skipped_no_unihan.json`). 이 중 실사용에 필요한 글자가 나오면 개별적으로 Unihan 갱신판을 재확인해 추가.
  - 새 표에 없는 8자(努 奴 姬 嫩 怒 港 珊 𮕩, 교육용 기초한자 근거로 유지)는 이 표의 누락인지 실제 배제 사유가 있는지 미확인 — 대법원 시스템에서 직접 재조회하면 확정 가능.
  - 이 새 표 자체도 원본이 아니라 파생/재정리본이라는 자기 신고가 있어(9,460 vs 공식 9,389), **법적 효력이 필요한 실사용 단계에서는 대법원 시스템 원본 재확인이 필요**하다 (7장과 일치).
  - `ETC/오행배속표.hwp`도 확인했으나 부수→오행 표가 **아니었음** (색깔·방위·오장육부 등 일반 오행 속성표 + 근거 불명확한 "성씨" 묶음뿐).
- **자원오행 배속표 (214부수 중 18개만 배속, `config.ts` `RADICAL_ELEMENT`) — "채워야 할 TODO"가 아니라 "구조적으로 완결 불가능한 항목"으로 결론 (2026.7.25 조사 완료, 3.5절 참고)**:
  - 조사 결과 "214부수 전체 배속표"라는 업계 표준 자체가 없다. 전문 작명원도 자체 표 없이 유료 자전을 참고하라고 안내하고, 자원오행 이론 자체를 비판하는 시각도 있다.
  - 실제로 山/石처럼 흔한 부수조차 출처 간 배속이 갈린다는 걸 石(金 vs 土)에서 확인해 표에서 뺐다(19개 → 18개).
  - 더 채우려면 "출처를 찾아 채운다"가 아니라 **"특정 참고서적·유파 하나를 선택해 그 책의 배속을 전부 그대로 따른다"는 결정**이 필요하다 (부분적으로 여러 출처를 짜깁기하면 지금과 같은 내부 모순이 또 생긴다). 이 결정을 내리지 않는 한 18개 상태를 계속 유지한다 — 이건 미완성이 아니라 의도된 최종 상태다.
  - 후보 순위·필터링 로직(Phase 4)은 `element`가 대부분(9,063자 중 2,979자, 약 33%) `NULL`이라는 전제로 설계해야 한다. `NULL`을 "자원오행 조건 불충족"이 아니라 "이 기준으로는 판단하지 않음(조건 건너뜀)"으로 취급할 것 — **부수 오행이 없을 때 글자 의미로 보완하는 2단계 로직은 만들지 않기로 확정**했다 (3.5절 참고. 이유: 의미 기반 오행 판단은 부수보다 더 유파 의존적이라 규칙화 불가 + LLM이 오행을 판정하게 되어 2.3과 충돌).
- **성씨 테이블 확장 (2026.7.28, 30개 → 100개/102행으로 완료)**: `etc/korean_sung.hwp` 기준 상위 100개 성씨로 확장했다(4.4 참고). 남은 갭: 이 100개 밖의 희귀 성씨·이체자(예: 100위 밖 성씨, 흔치 않은 한자 표기)는 여전히 없다. (`오행배속표.hwp`의 "성씨" 목록은 분류 근거가 불명확해 채택하지 않았음 — 기존 판단 유지.) 복성(남궁 외 제갈·황보·선우 등)은 이번 표에 남궁 하나만 포함되어 있어, 다른 복성이 필요해지면 별도 출처 확인이 필요하다.
- **4격(원격·형격·이격·정격) 계산의 이름 1글자 보정**: 현재 `calcSagyeok`은 이름이 1글자면 형격=이격이 되는 단순 합산만 적용. 일부 유파는 가상 획수를 더하는 보정을 쓰므로 검수 후 결정.
- **이름 점수 확인 서비스(3.10)의 1글자·3글자 이상 이름 미지원**: `GIVEN_NAME_LENGTH=2`와 같은 이유로 채점도 두 글자만 받는다. 위 4격 1글자 보정이 확정되면 함께 확장 검토.
- **결제(페이앱, 0.4·Phase 14)의 남은 갭**:
  - **결제 완료 후 취소/환불 자동화** — `payment_order.status`에 `canceled`/`failed`는 있지만, 관리자가 이미 `paid`인 주문을 취소·환불"실행"하는 버튼/API는 없다. 현재는 페이앱 관리자 사이트에서 직접 처리해야 한다. (결제 완료 *전* 요청 취소는 2026.8.5에 해결됨 — 0.4 "결제 확인 중 취소" 참고.)
  - **프리미엄 작명 공개 오픈 여부** — `app/naming/page.tsx`의 관리자 전용 게이트(정식 출시 전 임시 제한, 5장 Phase 6~)를 이번 결제 기능과 별개로 그대로 유지했다. 즉 지금은 결제 인프라만 구축됐을 뿐, 관리자가 아닌 일반 사용자는 여전히 `/naming`에 진입할 수 없다. 게이트를 언제·어떻게 제거할지는 별도 결정.
  - **삼성페이 미지원** — 페이앱 웹 결제(JS/REST)에는 삼성페이 옵션이 없다(대면결제 APP SDK 전용). 필요하면 페이앱에 별도 문의해 재검토.
  - **로컬 개발 환경에서 웹훅 테스트 불가** — 페이앱이 `localhost`로 웹훅을 보낼 수 없어, 실제 결제 흐름 검증에는 ngrok 등 외부 접근 가능한 URL(`APP_BASE_URL`)이 필요하다.
  - **가격 변경 이력 없음** — `price_tier.updated_at`만 갱신되고 과거 가격 이력은 남지 않는다. 필요해지면 별도 이력 테이블 결정.
- **로그인 베타의 의도적 제외 항목 (0.2, Phase 9)**: 비밀번호 재설정(이메일 발송 인프라 없음 — 문의 시 운영자가 수동으로 `password_hash` 갱신), 이메일 소유권 인증, 가입/로그인 rate limit(Vercel Firewall/WAF 레벨에서 거는 게 더 정확하다고 판단해 앱 코드에서는 보류). 소셜 로그인(카카오/네이버)은 Phase 10(0.3)에서 완료.
- **SNS 로그인(0.3, Phase 10)의 남은 갭**: 카카오는 이메일 동의 항목이 카카오 측 별도 심사 대상이라, 심사 전에는 이메일 없이 닉네임만 받을 수 있다(현재는 placeholder 이메일 + display_name으로 처리, 7장 참고). OAuth 프로필 이메일이 기존 계정과 일치하면 자동 연동하는데, 이는 두 제공자 모두 이메일을 검증된 값으로만 내려준다는 전제에 기반한 판단이다 — 이 전제가 깨지는 사례(예: 제공자 정책 변경)가 확인되면 재검토 필요. 계정 연결 해제(특정 소셜 계정만 분리)·복수 소셜 계정 연결 UI는 아직 없음.

---

## 7. 법적·윤리적 주의

- 작명 결과는 참고용이며, 실제 출생신고 가능 여부(인명용 한자·독음)는 관할 확인이 필요함을 명시한다.
- 사주·작명은 문화적·전통적 관점이며 과학적 예측이 아님을 오해 없이 전달한다.

---

## 8. 코드 구조

### 8.1 지켜야 할 단 하나의 원칙

**계산 로직(순수 함수)을 `lib/naming/` 한 곳에 몰아넣고, 이 폴더는 Next.js·Turso·LLM(Anthropic SDK) 어떤 것도 import하지 않는다.**

이 프로젝트에서 이 분리가 특히 중요한 이유: 작명 규칙(원획 계산, 발음오행 상생, 81수리 조합, 용신 도출)은 미묘하고 사람이 눈으로 검수하기 어려워 반드시 실수가 난다. 순수 함수로 격리해야 "특정 사주 → 특정 용신/후보" 를 테스트 코드로 못박아 검증할 수 있다. 동시에 이 폴더가 LLM을 import하지 않으면, 구조적으로 LLM이 사주·획수·수리 계산에 개입할 방법이 사라진다 (2.3 역할 경계의 물리적 강제).

### 8.2 폴더 구조 (현재 단계)

```
yourname/                 ← 프로젝트 루트 (CLAUDE.md 위치)
│  │  ├─ phonetic.ts  ← 발음오행: 초성→오행, 상생 판정
│  │  ├─ numerology.ts← 수리사격 계산, 81수리 조회 로직
│  │  ├─ elements.ts  ← 팔자 → 오행 분포 집계
│  │  ├─ yongsin.ts   ← 신강약 판정 + 억부 용신 도출 (규칙 엔진)
│  │  ├─ manseryeok.ts← 만세력 상세: 십신·지장간 정기·공망 계산 (3.8)
│  │  ├─ candidates.ts← 이름 후보 생성(탐색·다양성 선택) — 유료 전용 (Phase 4/6/8)
│  │  ├─ similarity.ts← 이름 두 글자 간 자모 유사도 판정 — 유사 이름 소프트 필터용 (3.6.1, Phase 8)
│  │  └─ score.ts     ← 이름 채점(등급 판정) — 무료 전용 (3.10, Phase 7)
│  ├─ saju.ts         ← @fullstackfamily/manseryeok 를 감싸 Saju 타입 반환
│  ├─ db.ts           ← Turso 접속 + 조회 함수 (한자·성씨·수리·한자 음 검색, 읽기 전용 참조 데이터)
│  ├─ auth.ts         ← 로그인 베타(Phase 9): 비밀번호 scrypt 해시, DB-backed 세션 발급/조회 (0.2).
│  │                     getCurrentUser()가 oauth.ts의 next-auth 세션도 함께 확인한다(0.3).
│  ├─ db-auth.ts      ← user/session/score_result/naming_result/oauth_account CRUD (getDbClient는 db.ts에서 재사용)
│  └─ oauth.ts        ← SNS 로그인(Phase 10): next-auth(Auth.js v5) 설정 — 카카오/네이버 프로바이더,
│                        jwt/session 콜백에서 db-auth.ts의 findOrCreateOAuthUser 연동 (0.3)
│
├─ scripts/db/        ← Turso 스키마 적용 + 참조 데이터 적재 스크립트 (1회성, Next 앱과 무관)
│  ├─ schema.sql, apply-schema.js
│  ├─ seed-hanja.js, seed-numerology.js, seed-surname.js, seed-given-names.js
│  └─ data/           ← 적재용 JSON (final_hanja.json, given_names.json 등). 원본 가공 과정은 etc/ 참고.
│
├─ app/page.tsx           ← 랜딩(두 서비스 카드로 분기, 계산 로직 없음)
├─ app/naming/page.tsx    ← 서버 컴포넌트(로그인 여부만 확인) + app/naming/NamingWizardClient.tsx(위저드 본체)
├─ app/score/page.tsx     ← 무료 위저드+결과 화면 (Phase 7, 로그인 게이트 없음)
├─ app/login/page.tsx, app/signup/page.tsx ← 로그인/회원가입 (Phase 9)
├─ app/mypage/page.tsx    ← 저장된 결과 목록(점수 확인 영구·작명 30일 이내), 로그인 필수
├─ app/mypage/score/[id]/page.tsx, app/mypage/naming/[id]/page.tsx ← 저장된 결과 상세(ScoreDashboard/ResultsDashboard 재사용)
├─ app/api/name/route.ts  ← 로그인 게이트(401) → saju·db·naming(candidates)·llm 순서대로 호출 → naming_result 저장. 계산 로직 없음.
├─ app/api/score/route.ts ← saju·db·naming(score) 순서대로 호출(LLM 미호출) → 로그인 시 score_result 저장.
├─ app/api/auth/{signup,login,logout}/route.ts ← 회원가입/로그인/로그아웃 (Phase 9)
├─ app/api/auth/[...nextauth]/route.ts ← SNS 로그인(카카오/네이버) OAuth 핸들러 (Phase 10, 0.3)
├─ next-auth.d.ts         ← next-auth Session/JWT 타입 보강 (내부 회원 id·닉네임 필드, 0.3)
├─ app/api/history/score/[id]/route.ts ← 저장된 점수 결과 삭제(DELETE) 전용
├─ app/api/hanja-search/route.ts ← "한자 찾기" 조회 전용, 계산 없음.
├─ app/mypage/inquiries/page.tsx ← 회원 문의하기(0.5, Phase 15) 등록/내역 화면
├─ app/api/inquiry/route.ts ← 문의 등록(POST) 전용
├─ app/admin/inquiries/page.tsx ← 관리자 회원문의관리 화면 (0.5, Phase 15)
├─ app/api/admin/inquiries/[id]/route.ts ← 문의 답변(PATCH)·삭제(DELETE) 전용
└─ app/admin/users/page.tsx ← 관리자 전용(ADMIN_EMAILS) 회원관리 조회 화면 (2026.8.1, 0.3 "관리자 회원관리 화면 신설" 참고)
```

- `lib/naming/` 안의 파일들은 서로는 import해도 되지만, 폴더 밖(Next/Turso/SDK)은 절대 import하지 않는다.
- `saju.ts`, `db.ts`, `auth.ts`, `db-auth.ts`는 외부 라이브러리·DB를 감싸는 얇은 모듈. "계층"이 아니라 그냥 파일 하나씩이다. `db.ts`(읽기 전용 참조 데이터)와 `db-auth.ts`(쓰기가 잦은 사용자 생성 데이터)는 성격이 달라 분리했다(Phase 9).
- 파이프라인 조립(팔자 → 오행 → 용신 → DB 조회 → 필터 → 순위)은 처음엔 `route.ts` 안에서 직접 한다.
- LLM 호출은 이 파이프라인 **바깥**, 후보가 확정된 뒤에만 붙인다.
- 마이페이지의 목록/상세 조회처럼 소비자가 Server Component뿐인 경우, 굳이 `GET /api/history/*` 같은 조회 API를 만들지 않고 `lib/db-auth.ts`를 페이지에서 직접 호출한다(8.3) — 클라이언트 인터랙션이 실제로 필요한 삭제만 API로 둔다.

### 8.3 언제 구조를 더 나눌 것인가 (판단 기준)

구조는 오직 두 가지를 위해 존재한다: **테스트하기 쉽게**, 그리고 **나중에 바꾸기 쉽게.**

새 폴더/계층 추가가 고민될 때 자문한다: "이걸 나누면 지금 당장 테스트가 쉬워지나, 아니면 곧 바뀔 게 뻔한 부분이 격리되나?" 둘 다 아니면 아직 이르다. **통증(예: route.ts가 너무 길고 지저분해짐)이 실제로 느껴진 뒤에 나누는 것이 거의 항상 옳다.**

구체적 트리거:
- `route.ts`의 파이프라인 조립부가 길고 재사용이 필요해지면 → `services/naming.ts`로 추출.
- DB 조회가 여러 곳에서 반복되고 형태가 복잡해지면 → `db.ts`를 리포지토리 형태로 정리.
- 지금은 위 둘 다 하지 않는다.

### 8.4 테스트

`lib/naming/`의 순수 함수는 `vitest`로 단위 테스트한다. 규칙이 미묘한 함수(`yongsin.ts`, `numerology.ts`, `phonetic.ts`)는 알려진 입력→기대 출력 케이스를 반드시 테스트로 고정한다.

### 9. 디자인 시스템

디자인 시스템(컬러 토큰, 타이포그래피, 컴포넌트 스타일, 레이아웃 패턴)은 별도 문서 design.md에서 관리한다. 화면·컴포넌트 작업 시 반드시 design.md를 먼저 읽고 그 안의 토큰과 규칙을 따른다. 색상·폰트·라운드 값을 코드에 하드코딩하지 말고 Tailwind theme.extend 또는 CSS 변수로 토큰화해 참조한다.